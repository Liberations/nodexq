# 节点象棋更新日志

## v1.0.1 - 2026-09-18 节点象棋魔改版

- **语音播报（TTS）**：新增“语音播报设置”（首页设置菜单，位于皮肤设置下方），可开关走子语音播报、调节语速（50%～200%）、选择系统提供的中文音色并试听。开启后每完成一步走子（对弈、自主分析、推演、错题棋盘及向前导航）用 Android 原生 TextToSpeech 播报，格式为“红/黑方前缀 + 棋子名后一次停顿、动作与数字连读”（如“红炮，二平五”“黑马，8进7”）；将军不重复 TTS，沿用系统 check.wav 音效。队列只保留最近两条完整招法，连走多手时至少最后两条能完整读出。初始化参照 tchess 实现：onInit 中验证简体中文语音数据，缺数据判为不可用并释放引擎；未就绪时挂起最新一条播报，就绪后补放。Android 11+ 已在清单声明 TTS_SERVICE queries。快速连续走子自动打断上一条，播报与“当前声音”音效开关相互独立。
- **语音走棋（离线 ASR）**：棋盘菜单新增“语音走棋”，基于 sherpa-onnx（Apache-2.0）离线流式识别，内置中文 zipformer-small int8 模型（assets/asr，约 26MB），无需联网。识别文本经**拼音模糊匹配**纠正为标准走法：内置象棋词汇拼音表（棋子/数字/进退平的同音字），把识别串与当前局面全部合法着法的中文记谱拼音做编辑距离匹配，容错同音字与错别字（如“码八进七”→马八进七、“泡二平舞”→炮二平五）；修复了最优候选筛选顺序 bug（更近候选未覆盖旧候选）。唯一命中直接走棋，多候选弹窗选择。运行时依赖 kotlin-stdlib 已补入。
- **语音走棋悬浮面板**：可开启常驻悬浮窗（TYPE_APPLICATION_OVERLAY，需 SYSTEM_ALERT_WINDOW 权限），面板两行：第一行状态徽章（待/候/录/停/载）+ 动态声波条（录音时大振幅波动）；第二行实时识别文字，匹配成功显示纠正结果（如“码八进七 → 马八进七”），失败面板内提示“未识别到”（不再弹错误 Toast）。轮到玩家自动延迟 3.5 秒收音（避开对方落子音效与 TTS 播报串音），30 秒超时；对方回合进入抑制态丢弃音频不喂识别器。麦克风与识别线程常开（端点判定说完一句后立即重开会话，不再每回合停启），点击面板在暂停/待命间切换，可拖动改位置，彻底关闭走菜单“悬浮球：开/关”。
- **盲棋训练**：首页新增“盲棋训练”入口（分析模式同款局面流程），棋子三态显示：**隐藏**（全部棋子完全不可见，将帅同样隐藏，不设例外）/ **轮廓**（所有棋子统一替换为 empty 轮廓图，只看得到有子位置）/ **显示**（正常皮肤），棋盘下方三个等宽按钮点击切换。进入时自动开启语音播报与语音走棋悬浮球。
- **盲棋读谱与人机难度**：棋盘底部“播放棋谱”点击后弹出间隔设置（默认 3 秒，1～60 秒可调），确认即从主线第一手顺序演示，按钮变为“停止播放”，播完或停止后恢复；演示只做逐手前进导航，走子音效与 TTS 播报由导航链路本身触发，同一手不会被读两次。“人机难度”按钮显示当前难度名（如“难度：略知一二”），点击列出全部 22 档，选择后实际作用于电脑执子链路（分析模式电脑走棋走 manualEngine：槽位、线程数与搜索限制均按档位下发，对弈引擎同步重配）。
- **棋钟**：首页新增“棋钟”入口，模拟线下比赛计时。规则弹窗按组提供预设：慢棋（60+30s、30+10s、50+20s）、快棋（25+5s、10+5s、5+3s）、超快棋（3+2s）、包干（30 分钟、20 分钟、10 分钟、5 分钟）、读秒（60+30 秒×5 次、20+10 秒×5 次、15+10 秒×5 次、10+10 秒×3 次）、附加赛（红 6 分+3s、黑 4 分+3s，和棋判黑胜）与自定义。按钟还原实体棋钟手感：开局按下先行方一侧对方钟开始计时；行棋方按钟结算顺序为“扣本步耗时 → 判超时 → 加秒/读秒重置 → 换边”，超时不能靠加秒救回；读秒制基础时间耗尽后进入每步固定读秒并按钟递减次数，次数用尽即超时。切换后 300ms 防误触锁定，当前行棋方卡片描边高亮，最后 10 秒变红并提示音一次。结束弹窗显示红方胜/黑方胜/和棋并可继续对局；外观可自定义背景色、字体颜色、字体大小（60%～160%），全部持久化。
- **皮肤系统增强**：外置皮肤支持可选 `empty.png|webp|jpg` 轮廓图覆盖（增删纳入皮肤缓存签名），缺失时回退内置 empty 资源（无盲棋专用目录，轮廓图跟随当前皮肤目录）；单个棋子图缺失时逐枚用内置 default 皮肤补位，不再整皮加载失败。文件名**大小写不敏感**（RK.PNG、Rk.webp 均可），并兼容 **w 前缀命名**：红方 wr/wn/wb/wa/wk/wc/wp 对应 rr…rp，可与标准名混用。
- **粘贴菜单合并**：棋盘长按菜单“粘贴”与“粘贴（天天象棋URL）”合并为“粘贴(支持天天象棋URL)”：自动从剪贴板混合文本提取第一条 http(s) 链接（兼容链接前后口令文字、标点、全角字符），含 QipuId 时直接调用接口载入，否则按棋谱文本继续识别。
- **关于对话框**：开源地址（https://github.com/Liberations/nodexq）改为可点击链接，直接跳转浏览器。
- 二合一与普通棋谱浏览在新增步数后自动跟随最新棋步，手动导航浏览仍保留原滚动位置。
- “加入错题本”改为保存当前局面及下一步的一步 PGN 到 correction 目录；错题条目打开后直接进入分析模式，分析模式已有棋谱时先询问是否覆盖。
- 评测二级页面新增“等级分变动历史”，记录并展示每局评测的等级分变化、对手等级分、K 值和连胜信息。

## V20.5 - 2026-09-17

- 新增“语音走棋”（棋盘菜单内）：基于 sherpa-onnx 离线语音识别（Apache-2.0，内置中文流式 zipformer-small int8 模型，无需联网），点击后说出走法（如“炮二平五”“马八进七”），识别文本与当前局面全部合法着法的中文记谱做模糊匹配（兼容数字/同音字/错别字）；唯一命中直接走棋，多候选弹窗选择，零候选提示重说。走子仍走原有合法性/送将校验链路。首次使用会申请麦克风权限，引擎在首次进入时后台加载。
- 新增“语音播报设置”（首页设置菜单，位于皮肤设置下方）：可开关走子语音播报、调节语速（50%～200%）并选择系统提供的中文音色，支持试听。开启后每完成一步走子（对弈、自主分析、推演、错题棋盘及向前导航）用 Android 原生 TTS 朗读中文记谱，将军时追加“将军”提示；播报与“当前声音”音效开关相互独立，快速连续走子时自动打断上一条播报。
- 棋盘长按菜单的“粘贴”与“粘贴（天天象棋URL）”合并为“粘贴(支持天天象棋URL)”：粘贴时自动从剪贴板混合文本中提取第一条 http(s) 链接（兼容链接前后附带口令文字、标点），提取到含 QipuId 的天天象棋链接时直接调用接口载入棋谱，否则按棋谱文本继续识别，不再单独提供独立菜单项。
- 二合一与普通棋谱浏览在新增步数后自动跟随最新棋步，手动导航浏览仍保留原滚动位置。
- “加入错题本”改为保存当前局面及下一步的一步 PGN 到 correction 目录；错题条目打开后直接进入分析模式，分析模式已有棋谱时先询问是否覆盖。
- 评测二级页面新增“等级分变动历史”，记录并展示每局评测的等级分变化、对手等级分、K 值和连胜信息。

## V20.4 - 2026-09-03

- **PGN 注释与标签转义**：读取 PGN 注释和标签时兼容 `\\n`、`\\r`、`\\t`、`\\\\`、`\\\"`、`\\/`、`\\uXXXX` 等文本转义，并统一换行符；修复来源文件把注释换行写为字面量 `\\n` 后在节点象棋中原样显示的问题。写出标签改为标准反斜杠转义，不再把双引号和反斜杠替换成其他字符；花括号注释继续写实际换行，符合 PGN 正文格式。东萍 UBB 的 `||` 换行、XQF 原始注释和会话 JSON 编解码保持各自既有规则。
- **PGN 粘贴逐手注释定位**：读取改为沿主线正文顺序解析着法和花括号评注，评注固定归属到它在原文件中紧邻的前一手；不再按“第几条评注”机械回填到“第几手”。修复含前段无注释、后段才开始评注的 PGN 在粘贴后把所有评注错移至开局数手的问题，同时跳过变例内部评注，并兼容旧版根注释前缀。
- **标准 PGN 标签、评注与结果**：保存 PGN 和棋盘长按“复制棋谱”统一保留导入棋谱已有的 `Event`、`Site`、`Date`、`Round`、`Red`、`Black` 标签；缺失字段再补节点象棋默认值。输出遵循中国象棋 PGN 的标准标签次序，根说明和逐手注释直接写入花括号，删除旧版私有的 `#1,1#`、`#0,0,0#` 前缀及空注释；棋谱最后一手后必定追加 `1-0`、`0-1`、`1/2-1/2` 或 `*`。读取同时兼容旧版带前缀文件与新版直接根注释，避免根说明被错误归入第一手。
- **顶栏与皮肤目录**：所有模式的顶部图标按钮、顶栏文字按钮及评测“提和/认输”按钮取消可见外框，保持原有填充、按下和高亮反馈。外部皮肤根目录自动创建 `.nomedia`，系统相册不再索引皮肤图片，目录读取和皮肤加载不受影响。
- **已安装皮肤的相册隐藏迁移**：启动后会为 `/nodexq/pic` 及其所有既有皮肤子目录补建 `.nomedia`，并将标记和现有 `.png`、`.webp`、`.jpg`、`.jpeg` 分批交给系统媒体扫描器重新索引，清理旧版本遗留的相册可见记录。皮肤继续保留在原目录，未移动、改名或删除；媒体扫描服务暂不可用时，`.nomedia` 仍会在下一次系统扫描时生效。
- **留存兼容**：未完成棋局留存新增导入棋谱标签字段；恢复会话后再保存/复制仍使用原有赛事、轮次、日期和棋手资料，旧留存记录继续可读。

## V20.3 - 2026-08-27

- **XQF 读写兼容与大型变招谱修复**：写出统一改为未加密 XQF 1.0 标准布局，按低版本规则固定写入根/走子注释长度、原样保留走子注释文本、主线/变招标志和终局结果字段，可与 cchess 等常见程序直接交换；读取补回仅含初始局面或根记录的文件，保留其 FEN、标题和胜负结果，同时继续兼容高版本加密、黑方首着和旧节点零密钥文件。读取 XQF 走法树时取消固定 20,000 节点截断，改按实际走法区可容纳的最小记录数设定安全边界，避免大型开局谱和深层变招在中途漏读；两份真实多分支棋谱的全部根到叶线路可与 cchess 往返对齐。
- **东萍 DhtmlXQ UBB 读、改、存与外部打开**：新增 `.htm` / `.html` UBB 棋谱支持，可识别 `binit` 残局初始局面、主线、任意层分支、初始说明和逐手注释，按东萍规则转换坐标、分支起点及 `||` 换行；保存菜单提供“东萍 UBB（.htm）”，并补充 `text/html`、`.htm` / `.html` 的文件关联及分享入口。严格导出时先完整写出全部 `move_*` 变例，再按东萍引用顺序写 `remark` 初始说明、主线注释和分支注释；导入变例 ID 会随编辑会话保留，未改动的谱按原标签顺序写回，新增分支才分配新 ID；旧版节点象棋误写的 `comment0` 可继续读取，保存时自动修正为标准 `remark`。
- **局势图拖动改为跟手浏览并修复松手闪退**：手指横向移动跨入新回合时立即重放并显示对应局面，拖动期间不重建局势图、不播放走子声音、不重启分析；松手只提交尚未预览的最后回合并更新图表当前位置，不再在 `ACTION_UP` 分发期间销毁和重建内容树，普通点击仍沿用完整导航刷新。
- **重新打分可选写入注释**：在“给所有分支打分”下新增默认“否”的双选项；选择“是”时，仅将本次实际获得有效评分的局面以固定首行写入根注释、主线与分支注释顶部，再次重打分会更新该首行而不重复叠加，原注释正文继续保留。
- **分析页面切换、异步保护与电脑出招**：二合一与非二合一棋谱/引擎页面切换仅更新显示布局，保留棋盘、当前局面、分支、分析缓存和正在搜索的引擎；搜索增加代际与异步回调保护，过滤旧 PV、旧 stdout 和旧 UI 回调，修复 `d0e1` 未执行着法、引擎卡住、放大镜超时及后续操作失效。电脑执红/黑的立即出招与变招始终作用于当前搜索，尚未进入可停止状态时会继续等待，电脑回合暂停分析时也不再弹出无意义提示。
- 自主分析模式点击“新建”时固定以红方先行，仅影响分析模式，不改变正式对弈和其他模式的棋盘方向设置。
- 评测结束流程统一为“返回棋盘 / 再来一局 / 退出”：返回棋盘仅查看终局，再来一局重新匹配并创建受限的评测棋盘，退出返回评测模式二级页面；终局页不显示“新建”，离开终局时清理评测内存和留存状态，二级页不会再把已完成棋局显示为“继续评测”。
- 评测棋盘提和/认输按钮达到可用回合后改用与“菜单/翻转”一致的灰色描边；点击“提和”先弹出确认框，确认后才启动提和评估，取消不会中断当前对局。
- 修复首页入口与统计按钮左右边缘显示不完整：清除系统 `Button` 的默认 tint/最小尺寸约束，并增加完整的 1dp 浅灰描边，保持原有按钮尺寸、圆角和布局间距。
- 修复皮肤/高亮色 HEX 输入首次点击“应用”后被亮度滑块取整的问题，输入的 6 位 RGB 色值现在一次即可准确生效。
- 评测等级分新增连胜加速：第 1～4 胜使用 `K=32`，第 5～9 胜使用 `K=64`，第 10 胜起使用 `K=108` 封顶。所有正常计分的评测胜利均累计连胜；和棋、失利或无效评测会清零，连败始终按 `K=32` 结算，连胜状态会跨重启保存。
- 首页顶部标题由版本号改为“节点象棋”。
- 修复评测随机难度与引擎执子方覆盖自选难度选择；自选难度现独立保存自己的难度和执子方，并在升级时迁移旧版选择，临时退出评测后直接开始自选对弈不会继承评测局面或评测专用档位。
- 首页“设置”弹窗标题增加隐藏维护入口：连续点击“设置”10次后，可打开等级分重置确认框；确认后将评测等级分恢复为最低值 500 并清零连胜，取消则不改变任何数据。
- 结构维护：V20.1 起持续把会话持久化、难度配置、评分、皮肤、棋盘页面、分析、编辑、分支和终局弹窗等职责迁出 `MainActivity`；主类从 1 万余行逐步降至当前 3,853 行，剩余代码主要负责跨控制器状态编排和 Android 生命周期收口。
- 应用版本升级为 `20.3`，versionCode 升级为 `56`。

## V20.2 - 2026-08-23

- 首页改为“评测模式 / 自选难度”双入口；自选难度页承载引擎执子、难度选择、历史战绩、难度名自定义和开始对弈流程。自选难度页面按钮顺序固定为“开始对弈、随机分配、难度名自定义、返回首页”，所有操作按钮统一尺寸。
- 新增评测模式：按玩家等级分在 ±150 区间匹配 22 档难度，距离越近权重越高；评测对局独立结算等级分，自选难度对局不影响等级分。玩家等级分初始为 500，最低不低于 500；参考段位文字同步更新。
- 评测模式使用 22 档实际引擎配置和固定等级分：520、765、875、985、1130、1210、1310、1360、1410、1535、1650、1770、1830、1890、2000、2110、2210、2300、2400、2600、2870、3290。
- 22 档中只有原有 13 档保留名称并显示在自选难度页（序号 1、2、4、6、8、10、12、15、17、19、20、21、22）；另外 9 档评测专用配置不再擅自命名，只按难度序号使用。旧版自选难度选择、战绩、自定义名称和留存棋局会按新版序号映射迁移。
- 针对更新后的 `libduf.so`，duf 人机难度改为通过 UCI 读取棋规选项，并默认下发 `Repetition Rule = github_AXF`；HCE、131 及通用外部引擎保持原有规则路径。
- 评测棋盘隐藏分析、新建、立即出招、变招、推演、引擎标签、局势图和注释列；提和前 25 回合、认输前 5 回合置灰；按钮默认使用底色，按下时短暂高亮。终局后恢复回溯、局势图、重新打分和推演，但禁止重新走棋。
- 评测入口按钮放在说明文字下方；评测棋盘侧滑返回评测模式二级页面，再次进入评测模式或返回首页不会丢失当前内存状态。自选难度棋盘侧滑返回自选难度二级页面。
- 评测未完成对局新增独立本地留存，不与自选难度/分析模式留存混用；应用暂停、销毁或从棋盘返回时保存。进入评测模式二级页面只显示页面，不弹窗；点击“开始评测/继续评测”时才提示“上次还有未完成的评测，是否继续？”。选择“继续对局”恢复，选择“新的对局”才清除旧局；点击弹窗外部、取消或侧滑均保留对局并停留在二级页面。
- 自选难度页隐藏版本号及分析模式、最近对局、设置入口；随机分配同步刷新难度和引擎执子显示，不弹出提示。评测对局保存到最近对局时使用难度序号，不使用汉字名称。
- 评测/自选难度页面返回路径、对局状态保持、随机分配、按钮统一尺寸及难度名自定义入口完成统一调整。
- 版本升级为 `20.2`，versionCode `55`。

## V20.1 — 2026-08-21

- 电脑执红/黑进入分析模式时后台预热共享 131 引擎，并预先完成新局握手；有限搜索不再在每手额外等待 `isready`，首次点击和连续走棋均直接进入 `position + go`。
- 分析模式菜单的“棋谱引擎二合一”改为半宽，右侧保留同宽空位；行棋设置的多选圆点改为与重新打分方向一致的蓝色选中、灰色未选中样式。
- 版本升级至 V20.1（versionCode 54 / versionName 20.1）。
- 修复 Elo 胜负方向：等级分与历史战绩统一按玩家执红/黑判定，低分电脑取胜时玩家必扣分；1220 分负于 250 分难度约扣 32 分。
- 修复电脑执红/黑逐手重新应用 UCI 设置导致 131 引擎反复重启的停顿；设置只在首次开启电脑执子时加载，后续连续走棋复用进程。默认每步时间改为 0.1 秒。
- 青云对弈新增按执子方和回合动态调整 DrawRule：青云执黑时第 1～25 回合使用 `drawrepasredwin`，青云执红时第 1～25 回合使用 `drawrepasblackwin`；第 26 回合起统一恢复 `none`。日志会记录当前青云执子方、回合范围及实际下发的 DrawRule 状态。
- 修复等级分结算的认输胜负判定：认输直接传入明确胜负结果，不再受提示文字影响；整数等级分遇到极小 Elo 变动时至少增减 1 分，避免实际对局显示不变。
- 自主分析的电脑执红/执黑按钮按下后持续高亮，切换回对弈或新建时立即同步清除高亮；工具栏固定为翻转、黑电脑、红电脑、放大镜。
- 行棋设置的每步时间支持小数秒；电脑执红/执黑复用放大镜中文分析格式，连续电脑走棋移除额外 UI 延迟；菜单将行棋设置放到箭头显示右侧，两个入口等宽排列。
- 修复放大镜取消后分析内容冻结快照残留到电脑执红/执黑的问题：切换电脑执红/黑时立即解除冻结，清空旧快照并恢复实时 131 引擎内容。
- 修复部分鲨鱼 XQF 文件头部把黑方首着标为红方导致只读取半盘的问题：读取时按首个合法节点自动识别行棋方；节点象棋导出的 XQF 额外保存分支标签元数据，重新打开可恢复 A/B/C 线路编号。
- PGN 保存编码改为 GBK（中文 Windows ANSI），兼容鲨鱼象棋读取中文标签和注释。

- 新增引擎分析信息“只显示最新深度”开关并持久化，优化引擎设置行对齐；引擎设置说明补充局面分析、重新打分及电脑执红/执黑共用范围。
- 新增玩家等级分与 Elo（K=32）独立结算：默认 1200，实际值与手动设置值均限制在 50～4000；13 档难度使用固定等级分。等级分设置页支持修改数值及首页显示开关，并列出参考段位和不计分条件。
- 等级分不计入以下对局：第 25 回合内提和、悔棋、放大镜分析、变招/推演、进入分析模式、编辑局面，以及从分析模式切回对弈模式；其他符合条件的认输、绝杀、胜负和提和均按 Elo 公式更新。
- 等级分与历史战绩均增加单局幂等保护：终局后导航、回溯或赛后分叉再次形成胜负时，不会重复更新玩家等级分或历史战绩。
- 自主分析模式新增电脑执红/执黑、131 引擎通用行棋限制设置及菜单入口，支持深度/节点/时间组合 go 命令；补充电脑搜索忙碌态和代际保护，避免思考期间误落子或旧回调串台。

## V20.0 — 2026-08-19

- **修复高频“变招 / 导航 / 立即出招”交替时的分析串台竞态**：定位到普通换局面停止分析时，旧实现发送 `stop` 后会立即把分析读取循环置为结束；读取线程可能因此在真正收到旧搜索的 `bestmove/nobestmove` 前退出，而下一局面已经发送新的 `position + go`。极小概率下，迟到的旧 `bestmove` 会落入新搜索的 stdout 队列，被误认为新局面的结束结果，表现为新页面长期停在“分析中…… / 正在等待当前局面的主选着法”，继续操作也不跟手。
- **普通 stop 改为严格排空旧搜索**：现在只要 `go` 已发出，普通停止会保持原 stdout 读取线程存活，直到消费到对应的 `bestmove/nobestmove` 后才允许下一代搜索接管；旧搜索的最终着法仅用于终止握手，不会回调到新一代界面。“立即出招”仍保留原有 `stop → 最终 bestmove → 落子` 快速路径。若少数异常引擎在 stop 后长期不返回终止标志，会在短超时后直接重建进程，避免状态继续错位。
- **封闭 stop 与 go 的启动窗口**：新增轻量分析状态锁，把“确认请求仍有效、标记搜索活跃、发送 `go`”合并成原子短区间；`stopAnalysis()` 走同一状态锁，避免高频导航/重启恰好插在状态检查与 `go` 写入之间，造成已经取消的旧分析仍迟到启动。
- **新一代分析在 UI 侧立即收束旧搜索**：`startManualAnalysis()` 不再等后台启动队列排到以后才停止上一代搜索，用户点变招、前后导航或连续切换局面时会马上让旧搜索进入排空阶段，缩短高频操作下的等待窗口。
- **立即出招与变招交替采用最新操作语义**：点“变招”会取消上一瞬间尚未完成的立即出招待执行请求；若立即出招已经发过 `stop`，随后变招/导航只把这次 stop 转为普通排空，不再重复发送第二个 `stop`。反过来，若“立即出招”撞上正在重启的新分支/新局面，则保持 pending 并持续重试，直到新一代 `go` 真正开始后再 stop，不会误停上一代搜索后把请求卡死。
- **readyok 超时的连带问题一并处理**：上述串台会让后续 `isready/readyok` 同步处在旧搜索尚未真正收束的状态，是偶发“等待 readyok 超时、再点一次放大镜又恢复”的主要潜在来源。本版先保证搜索代际严格串行，并在异常 stop 无终止反馈时主动重建，避免把错误状态带入下一次 ready 同步。
- 版本升级为 V20.0（versionCode 53 / versionName 20.0）。

## V19.9 — 2026-08-19

- **引擎分析优势/劣势信息颜色支持自定义**：皮肤设置新增“己方优势信息”“己方劣势信息”两项颜色，可分别覆盖“深度…己分…NPS…WDL…”分析信息行原有的蓝色/红色；这两项不增加透明度。恢复默认时会删除自定义值，继续按原逻辑根据局势自适应显示优势蓝、劣势红。
- **红黑双方实际着法颜色支持自定义**：皮肤设置同步新增“红方实际着法”“黑方实际着法”颜色，覆盖引擎分析 PV 中双方实际着法文字；同样保留默认自适应/主题文字配色，并可通过恢复默认一键回到原逻辑。
- **首页设置入口调整**：首页“设置”新增“皮肤设置”和“难度名自定义”，皮肤设置现在可从首页与对局内菜单两处进入；首页设置不再显示“引擎设置”。
- **修正引擎选项翻译**：`drawrepasredwin` 由错误的“和棋循环黑胜”修正为“和棋循环红胜”。
- **动态校准改为独立临时页面**：皮肤设置主页面仅保留“开始动态校准”。点击后进入全屏临时校准页，使用更大的棋盘，并集中显示恢复默认位置、棋子大小、四向微调、微调步长、角点状态及动态校准说明；棋子大小拖动条由皮肤设置主页面移入校准页，拖动时实时刷新预览。完成后把校准结果（网格与棋子大小）带回皮肤设置，仍由皮肤设置“保存”统一持久化。皮肤放置与命名说明（default 为内置皮肤、pic 目录、board/棋子文件名、可选 back 背景图）从校准页移到皮肤设置“选择皮肤”上方。
- **人机难度扩展为 13 档并重做低档参数**：新增“独孤求胜、雾里看花、初悟棋道、初露锋芒”，前五档全部脱库 5 回合并使用 HCE：独孤求胜 `go depth 1`；略知一二/雾里看花/初悟棋道/初露锋芒分别为 `go nodes 400/1200/2400/4000`。棋摊中坚、街头霸王、县镇好手、业余顶尖、特级大师参数保持原值；“人类+”改名“天下无敌”且参数不变；青云保持不变。市县好手搜索预算由 15000 节点调整为 7000。
- **旧难度存档兼容迁移**：首次运行 V19.9 时，会把 V19.8 的 9 档难度索引、历史统计及留存棋局中的难度索引迁移到对应的新 13 档位置，避免扩档后旧数据错位；全新安装默认从第 1 档“独孤求胜”开始。
- **难度条改为 7+6 两行布局**：第一行显示 7 档（独孤求胜～街头霸王），第二行显示其余 6 档，从“县镇好手”开始。选中态不再使用白点，只有当前选中的档位显示其对应的 1～13 圆圈序号，其他档位不显示序号；10～13 同样使用程序绘制圆圈与数字，不依赖缺字风险较高的 Unicode 圈号字符。
- **难度名称支持持久自定义**：首页设置新增“难度名自定义”，第 1～12 档均可自行命名，“青云”固定不可修改；自定义名称会持久保存，输入留空或“全部恢复默认”即可恢复内置名称，并同步用于首页难度条、玩家名称及对局日志等显示。
- 版本升级为 V19.9（versionCode 52 / versionName 19.9）。

## V19.8 — 2026-08-18

- **对弈中使用放大镜仍正常计入历史战绩**：正式人机对弈点击“分析”不再把当前棋谱永久标记为分析专用；“立即出招”同样不再取消战绩资格。只有真正切换到自主分析模式、粘贴外部局面/棋谱等改变棋谱性质的操作，才继续按原规则不计历史战绩。终局仍保持单盘只结算一次，最近对局保存逻辑不变。
- **修复放大镜与电脑回合错位、分析迟滞**：正式对弈走到棋谱末端且轮到电脑时，放大镜实时分析会自动暂停，避免与正式对弈引擎同时争用 CPU；电脑落子并轮到玩家后自动按新局面重新启动，无需再次点击。分析按钮保持开启态，界面明确显示“电脑思考中，轮到玩家后自动继续”。关闭放大镜也不再递增与实战引擎共用的操作代数，避免电脑正在思考时把其正常 `bestmove` 回调误判为过期。
- **拦截迟到的旧局面分析任务**：为放大镜启动增加独立请求代数，并在引擎桥接层增加启动守卫；排队中的旧请求会在等待上一条搜索结束、创建分析线程以及真正发送 `position/go` 前再次确认是否仍属于当前局面。新建棋局、导航、走子或电脑回合暂停后，旧请求不能再迟到启动，从源头处理“新局第一次分析失败、再点一次才恢复”的竞态。
- **未终局时不再生成复盘优劣箭头**：正式对局结束前，`★ / 优 / 中 / 差 / 错` 及赛后 PV1 推荐箭头强制隐藏；放大镜只显示当前实时 PV 提示。对弈中的放大镜也不再并发刷新走前复盘数据、不再覆盖固定 100ms 的正式复盘评分来源，避免提示与赛后优劣判定互相污染；终局后仍按 V19.7 的固定评分/补齐流程显示复盘箭头。
- **首页“引擎执红 / 引擎执黑”外框收紧**：两项外层容器由 4dp 包边压缩为 2dp，整体高度由 56dp 收紧为 52dp；外层底色改为与首页其它按钮完全相同的“首页按钮色”。未选中项使用透明内层直接露出外框底色，选中项叠加全局高亮色；文字对比度继续按外层首页按钮色与内层高亮色的实际叠加结果动态计算。
- 版本升级为 V19.8（versionCode 51 / versionName 19.8）。

## V19.7 — 2026-08-17

- **首页汉字按实际背景自动切换高对比文字色**：首页版本号、分区标题、引擎执子、难度文字、历史战绩和入口按钮不再固定黑字。无 `back` 时按全局底色计算；存在 `back` 时按每个控件在背景图上的实际位置取样，半透明首页按钮/高亮按钮会先与取样背景做 alpha 合成，再按亮度自动选择深色或白色文字；首页滚动时按帧合并重新取样，保证控件移动到背景图的新区域后也会更新文字色。取样不进入 `onDraw` 热路径。
- **重新打分新增“给所有分支打分”**：位于“打分方向”下方，使用“否 / 是”圆形单选按钮，默认“否”，两项由同一 `RadioGroup` 严格互斥。选择“是”后范围上限按包含分支后的最长线路计算。
- **所有分支改为串行遍历评分**：主线保持原有顺序，之后按分支树深度优先逐条处理；每次只让同一个 `rescoreEngine` 搜索一个局面，禁止同时启动多个分支搜索，避免多引擎并发导致卡顿。分支自己的评分、Mate、完整性标记和 PV1 推荐均写回各自 `ManualVariation`，嵌套分支同样处理；分叉前局面的推荐会复用父线路结果，减少重复搜索。
- **修复正式人机对弈结束后回看没有优劣箭头**：正式对弈原本已有固定 100ms 局势评分，但没有把同一次搜索的 PV1/bestmove 保存为下一手复盘推荐，也没有把这组同源评分标记为可用于优劣提示，导致终局后往前翻只能看到局势分、看不到 `★/优/中/差/错` 与推荐箭头。现在直接复用对弈局势评分的同一次搜索生成“走后评分 + 下一手走前推荐”；若推荐早于下一手落子返回，会按走前局面暂存并在落子时写入，避免因为数组下标尚不存在而丢失。终局后只再串行补齐被 stop/覆盖的缺口；无需切换分析模式重新打分即可回看优劣提示。
- **终局复盘补齐仍保持低并发**：终局补评分/推荐继续使用局势评分引擎逐点串行补齐；终局胜负点按规则结果写入并保留复盘完整性，补齐过程中会原地刷新当前回看的优劣提示，不重建棋盘。
- 版本升级为 V19.7（versionCode 50 / versionName 19.7）。

## V19.6 — 2026-08-17

- **全局底色新增透明度**：皮肤设置在“全局底色”下增加 0%～100% 透明度拖动条并持久保存。无 `back` 背景图时根页面仍以不透明的底色 RGB 兜底，避免透明后直接露出不可控的系统窗口底色；存在 `back` 时，棋盘两侧、玩家信息行、工具区域、注释框、导航/操作键等底色表面使用该透明度作为覆盖层，可自然透出背景图。
- **新增“首页按钮色”及透明度**：入口位于“全局高亮透明度”下方，支持色盘、hex 色值输入与 0%～100% 透明度。默认沿用 V19.5 首页未选中按钮的 `#EFEFE9 / 100%`。该变量统一控制未选中的“引擎执红/执黑”、未选择的难度条、“开始对弈 / 分析模式 / 最近对局 / 设置”、历史战绩区域与统计/重置框，以及最近对局页面的每条记录框。
- **首页按钮与文字重新统一**：“开始对弈”不再使用全局高亮色，与其它首页入口按钮统一读取“首页按钮色”；首页版本号、分区标题、执子选项、难度文字、历史战绩及四个入口按钮的文字统一为黑色。
- **底色系操作键统一**：棋谱/引擎/局势图导航键、局势图中的“重新打分 / 报告 / 错”键统一跟随全局底色，不再使用高亮色；导航键与报告键不可用时只把文字改为灰色，背景保持底色不变。前后导航文字由“前 / 后”改为 `← / →`；分支列表中未选中的分支也改为底色。
- **棋谱 / 引擎 / 局势图标签取消高亮底块**：三个标签切换时不再使用全局高亮效果，仅在当前标签底部绘制一条短黑色指示线。指示线使用轻量 Drawable，仅重绘标签自身，不引入图片解码或内容区重建。
- **默认颜色恢复扩展**：“恢复默认色值与透明度”现在同时恢复底色 `#F6F4EE / 100%`、高亮 `#2A5C46 / 100%`、首页按钮 `#EFEFE9 / 100%`，以及既有红/黑箭头和局势图优势线颜色。
- **背景/交互性能保持**：继续沿用 V19.5 的 `back` 图片单线程异步解码与缓存；本版新增的透明覆盖、首页按钮色和标签短线均只使用已有颜色值/Drawable，未把解码、文件读取或整页重建加入点击/绘制热路径。
- 版本升级为 V19.6（versionCode 49 / versionName 19.6）。

## V19.5 — 2026-08-17

- **皮肤设置顺序微调**：将“棋子大小”拖动条移动到“选择皮肤”之后、“动态校准”之前；切换皮肤时仍会同步读取该皮肤独立保存的棋子大小并刷新预览。
- **重新打分每步用时增加记忆**：首次仍默认 `0.1` 秒；用户填写合法时间并点击“确定”后持久记住该值，下次打开“重新打分”参数弹窗自动回填上次时间，不再每次重置为 `0.1`。取消或输入非法值不会覆盖已记住的时间。
- **全局底色统一并支持自定义**：新增单一 `global_background_color` 作为页面基础底色来源，皮肤设置中的入口位于“动态校准”与“全局高亮色”之间；无背景图时，玩家信息行、棋谱/引擎相关基础区域、注释框等统一跟随该底色。红/黑方“红方/黑方 · 玩家/电脑 · …”不再保留独立底色，玩家信息行高度进一步压缩到 24dp。
- **全局背景支持皮肤 `back` 图片**：读取 `/storage/emulated/0/nodexq/pic/<皮肤名>/back.png|webp|jpg|jpeg`；存在可读取图片时作为全局背景，原本由全局底色覆盖的主要表面改为透明以透出同一背景；不存在或解码失败时自动回退全局底色。背景图片采用按屏幕尺寸采样、单线程后台加载以及“皮肤名 + 文件大小/修改时间”缓存签名，不在绘制/刷新热路径反复解码。
- **底色文字自动保证可读性**：纯色底色会按实际亮度自动选择深色或白色主文字，次要提示文字也按底色动态生成；透明高亮文字会先按高亮叠加到底色后的实际颜色计算对比度，避免深浅底色下文字看不清。自定义背景图片不做自动取色覆盖用户图片。
- **颜色输入统一改称 hex 色值**：所有颜色选择器的输入框及校验提示统一使用“hex色值”文案，仍接受 6 位十六进制色值及可选 `#` 前缀。
- **全局高亮色新增透明度**：皮肤设置增加 0%～100% 透明度拖动条，0% 为完全透明；V19.4 原有颜色以及 V19.5 新增底色被固定为默认值，新增“恢复默认色值与透明度”按钮，可一键恢复全局底色、高亮色/透明度、红/黑箭头色和局势图红优/黑优线色。
- **大系统字体保护**：针对部分 Android 用户把系统字体调到极大的情况，应用内字体倍率设置温和上限 1.18，系统“显示大小”仍正常生效；新增/调整的设置行优先使用 `wrap_content`，避免固定行高在大字号下把文字裁掉。
- **新增“引擎输出步数（0为不限制）”**：入口位于引擎设置，但属于应用显示参数，不向 UCI 引擎发送 `setoption`。0 保持完整 PV 显示；N>0 时每个深度、每条 MultiPV 只翻译并显示前 N 步。内部仍保存完整 PV，因此棋盘分析箭头、评分和引擎搜索逻辑不受截断影响。
- **交互性能复查**：保留 V19.1/V19.3 已有的皮肤 Bitmap 共享缓存与编辑面板局部刷新；本版新增背景图加载同样避开主线程和 `onDraw`，并使用单线程队列/请求代数丢弃过期任务，避免切皮肤、返回页面或连续操作时重复解码、重建造成按钮迟钝。
- 版本升级为 V19.5（versionCode 48 / versionName 19.5）。

## V19.4 — 2026-08-16

- **复盘推荐箭头与分析箭头统一按行棋方配色**：重新打分回看及放大镜分析中的推荐招法不再固定使用绿色；红方推荐招使用红方箭头色，黑方推荐招使用黑方箭头色，并与实时分析箭头读取同一组可配置颜色。
- **复盘隐藏实际着法箭头**：当前步的实走棋步已有四角框标记，因此 `★ / 优 / 中 / 差 / 错` 回看不再重复绘制实际着法箭头；等级徽标仍锚定在实际落子位置，仅当推荐招与实走不同才显示推荐招法箭头。
- **皮肤设置重新排序并新增四组颜色自定义**：动态校准完整模块及说明移到最前，其后依次为全局高亮色、棋子大小、红/黑双方箭头颜色、局势图红优/黑优线条颜色。新增颜色项全部复用 HSV 色盘、亮度条与 6 位 RGB 代码输入方式，并持久保存。
- **动态校准说明改为直白操作说明**：按新版文案完整说明黄色网格、四角控制点选择/取消/切换、方向键微调、整体拖动以及 `/storage/emulated/0/nodexq/pic/` 皮肤目录与固定图片命名规则；校准微调步长默认值由 `5dp` 改为 `2dp`。
- **自定义箭头颜色覆盖所有棋盘分析场景**：主棋盘、临时推演和错题推演统一应用红/黑箭头设置；第 3/4 步浅色续着会由所选基础颜色自动生成同色浅色版本。
- **局势图优势曲线支持自定义颜色**：红优与黑优的绘制线条分别读取用户设置，保存后当前局势图立即刷新。
- 版本升级为 V19.4（versionCode 47 / versionName 19.4）。

## V19.3 — 2026-08-16

- **修复编辑页面所有高频交互的短暂停顿**：定位到编辑模式中棋子按钮、删除/清空/初始、红先/黑先以及棋盘摆子等操作会反复调用 `updateGameContent()`，导致整个编辑面板 `removeAllViews()` 后重新创建。V19.3 改为进入编辑模式时只创建一次面板，后续交互只原地更新 14 个棋子格的数量、透明度、选中高亮以及删除/先手按钮状态，不再重建整棵 View。
- **编辑棋盘变化不再排队重建页面**：`onEditBoardChanged()` 从主线程 `post(updateGameContent)` 改为当前帧直接执行轻量状态刷新；摆子、移动、双击删除、清空和恢复初始局面后，控件计数与高亮会立即同步。
- **外部皮肤编辑图标复用共享 Bitmap 缓存**：编辑面板不再对当前外部皮肤的 14 张棋子图片逐张 `BitmapFactory.decodeFile()`；改为复用棋盘皮肤已经加载的进程级共享 Bitmap，避免进入/刷新编辑界面时再次产生磁盘读取和图片解码开销。共享 Bitmap 只读使用，不由编辑按钮回收。
- **编辑模式按钮切换改为局部刷新**：选择/取消棋子画笔、删除模式以及红先/黑先切换均不再触发整页内容刷新；棋盘只做必要的 `invalidate()`，保持原有单击选中、再次单击取消、双击删除等交互逻辑不变。
- 版本升级为 V19.3（versionCode 46 / versionName 19.3）。

## V19.2 — 2026-08-16

- **优劣图标改为棋盘最顶层绘制**：`★ / 优 / 中 / 差 / 错` 现在在棋子、分析箭头、PV 序号、上一手/选中四角框以及校准网格之后最后绘制，避免落子左上角的等级图标再次被绿色四角框遮住。
- **皮肤动态校准增加方向键微调**：保留原拖动四角点与拖动整张网格的方式，同时四个角点新增持久选中状态。轻点左上/右上/左下/右下角点即可选中并略微加粗，再点同一点取消，点击其他角点自动切换；下方新增 `↑ / ← / ↓ / →` 微调键，保持“第一行 ↑、第二行 ← ↓ →”的键盘布局；步长输入框直接放在方向键组右侧，默认 `5dp`，支持用户输入大于 0 且不超过 100 的小数步长。拖动与方向键共用同一四边形边界约束。
- **全局高亮色支持直接输入 RGB**：色彩盘页面新增 6 位 RGB 输入框与“应用”按钮，例如可直接输入 `395E42`；输入允许可选的 `#` 前缀，保存时再次校验，非 6 位十六进制颜色会 Toast 提示并保持弹窗不关闭。色盘、亮度条与 RGB 输入会相互同步。
- **最近对局显示回合数**：最近对局列表在时间右侧增加 `X回合`。按象棋通常口径，红黑双方各走一着计 1 回合，最后只有单方一着时向上取整；回合数从 XQF 主线读取，并按文件大小/修改时间做进程内缓存，避免重复打开列表反复解析同一棋谱。
- **DrawRule 枚举中文化补充**：仅在“和棋棋规（DrawRule）”组合框中将 `none` 显示为“无”，实际下发给引擎仍保持原始 UCI 值 `none`，不会影响其他选项的枚举文本。
- **放大镜分析不再清除重新打分提示，并可动态刷新**：当前步已具备完整重新打分数据时，开启放大镜会立即保留实际着法、PV1 推荐着法及等级图标。实时 `go infinite` 持续刷新该步“走后评分”；同时使用独立分析引擎对该步“走前局面”做一次短刷新，更新走前评分与 PV1 推荐首着，因此 `★/优/中/差/错` 和推荐箭头会随新分析结果更新，而不是一开启分析就消失。停止分析后保留更新后的提示数据。
- **编辑模式双击删除棋子**：普通编辑移动模式下，同一棋子在系统双击时间窗内连续点击会直接删除；第一次单击仍按原逻辑选中，超过双击时间后再次单击同一格仍按原逻辑取消选中，不与原单击交互混用。
- 版本升级为 V19.2（versionCode 45 / versionName 19.2）。

## V19.1 — 2026-08-16

- **修复进入棋盘/新建对局时的短暂卡顿**：定位到皮肤功能引入后的重复 Bitmap 解码——`ChessBoardView` 构造时先同步解码 default，`showGameScreen()` 随后又立即按当前皮肤再解码一次；一套 default 棋盘与 14 张棋子解码后的像素量约 7MB，连续两次分配/回收会触发明显主线程抖动。V19.1 现在允许“延迟加载皮肤”的棋盘构造方式，所有随后会立即应用当前皮肤的页面均不再先解码一次 default。
- **当前皮肤后台预热与单套缓存**：首页显示后在后台预热当前皮肤；内置 default 在进程内只解码一次并供棋盘页/推演页/皮肤预览共享。外部皮肤只缓存最近使用的一套，并以 15 张图片的文件大小与修改时间作为签名，文件发生变化后自动失效重读，避免“缓存越积越多”。外部皮肤不可读时会提前准备内置 default 作为 fallback。
- **新建对局不再重建整页**：在已经处于相同对弈模式/分析模式时点击“新建”，直接复用当前 `ChessBoardView` 与整棵页面 View，只重置棋盘数据、导航/分析状态和内容区；不再销毁页面后重新创建棋盘和重新加载皮肤。
- **留存棋谱删除改为异步写盘**：`clearSavedSession()` 从同步 `SharedPreferences.commit()` 改为 `apply()`，移除点击“开始对弈 / 分析模式 / 新建”时等待磁盘 fsync 的主线程阻塞。
- **分析引擎停止不再争抢长时对象锁**：排查到 `startAnalysis()` 原先以 `synchronized` 包住最长 1800ms 的“等待上一条 bestmove”流程，用户恰好在启动/重启分析时点“新建”或切模式，主线程调用 `stopAnalysis()` 可能等待同一 monitor。现将显式 stop 改为无对象锁的快速路径，并增加停止代数，保证启动窗口内发生取消时迟到的分析不会重新跑起来；内部重启仍保留 stop→等待 bestmove 的串行安全逻辑。
- **彻底修复长棋谱滚动与导航键重叠**：撤销 V19.0 的自定义 `VerticalClipScrollView.dispatchDraw()` 人工裁剪方案，恢复系统 `ScrollView` 的正常滚动绘制/命中坐标；改由棋谱页面根布局严格隔离 32dp 导航行和下方内容行，使棋谱内容无论滚动到何处都不能越过内容行顶部覆盖导航键。普通棋谱页与“棋谱+引擎”二合一布局同步处理。
- **修复滚动后下方棋谱不可见但仍可点击**：由于不再用固定画布裁剪区域干预 `ScrollView`，滚动后的可视区域与触摸定位重新保持一致；同时继续保留棋谱长文字在内容行内部向右适度溢出的原有行为。
- **优劣标记移至棋子左上角**：重新打分回看时，实际着法箭头仍按原逻辑显示，但 `★ / 优 / 中 / 差 / 错` 不再位于箭头末端，而是以实际落子后的棋子为锚点显示在左上角。圆形等级标记大部分位于棋子轮廓之外，减少对棋子文字的遮挡；位置会跟随皮肤校准坐标和棋子大小设置，并限制在棋盘图片范围内。
- 版本升级为 V19.1（versionCode 44 / versionName 19.1）。

## V19.0 — 2026-08-16

- **新增全局高亮色设置**：在“皮肤设置”中加入“全局高亮色”，点击后弹出 HSV 色彩盘并提供亮度拖动条，可自由选择应用内选中区域与主要高亮按钮的颜色；棋谱当前步、页签、导航/主要操作按钮、分析选中态、难度条、编辑模式选中项、最近对局相关高亮等统一读取同一颜色。浅色高亮会自动切换为深色文字，保证可读性。
- **精简皮肤设置**：移除顶部重复的“当前皮肤: xxx”一行，保留下面的“选择皮肤”作为唯一皮肤选择入口；默认内置皮肤及外部用户皮肤读取逻辑保持不变。
- **重新打分逐步优劣提示**：重新打分除 PV1 评分外，同时按 ply 保存“该步走前局面”的 PV1 推荐首着。浏览棋谱时，仅当当前步的走前评分、走后评分和推荐首着都拥有完整重新打分数据时才显示提示；实际着法始终绘制一支箭头，若 PV1 推荐不同则另绘制一支绿色推荐箭头，切换棋步后只保留当前步提示。
- **优劣等级与绝杀处理**：实际着法箭头末端按现有报告中的同一 L 损失算法显示等级：`L <= 50`（含负数）为绿色 `★`，`50 < L <= 150` 为“优”，`150 < L <= 500` 为“中”，`500 < L <= 1000` 为“差”，`L > 1000` 为“错”。Mate/绝杀沿用报告评分器的等效分与绝杀距离衰减逻辑，规则终局 ±6000 按 M1 处理，避免单独再造一套判断标准。
- **提示数据防串用**：新增重新打分来源标记并随棋谱留存/分支一同保存；实时分析或普通 100ms 局势评分若改变某一评分点，会撤销该点的“纯重新打分”标记，避免把不同来源的数据拼成错误的优劣提示。留存 schema 升级为 6，并保留旧记录兼容。
- **修复棋谱滚动覆盖导航键**：棋谱滚动区改为只在纵向严格裁剪、横向仍允许长招法文字适度溢出；向下浏览较长棋谱时，滚出顶部的棋谱框不再绘制到导航键区域。棋谱+引擎二合一布局同步修复。
- 版本升级为 V19.0（versionCode 43 / versionName 19.0）。

## V18.9 — 2026-08-16

- **MultiPV 重新打分评分修复**：保持用户 UCI `MultiPV` 设置不变，重新打分与初始局面补评时仅接受 `multipv=1` 的主变评分和深度，PV2/PV3/PV4…不再覆盖局势图数据；修复 4PV 等模式下“实时分析显示第一选择分数，但重新打分后误读最后一个 PV 分数”的问题。
- **三页面导航统一**：棋谱页、局势图页以及棋谱+引擎二合一页的四个导航键统一改为与非二合一引擎页相同的整行宽度、位置和高度；棋谱页“初始局面”与后续棋谱记录整体下移一行，二合一布局同步处理。
- **局势图控制区换行**：局势图第一行固定为整行导航，`重新打分 / 报告 / 错` 移到第二行；重新打分进行中的“停止 + 进度”同样固定在第二行。
- **新增皮肤设置**：菜单加入“皮肤设置”，第一行显示 `当前皮肤: xxx`，提供棋子大小拖动条和皮肤选择；`default` 始终为 APK 内置皮肤，用户无需外部目录即可直接使用。只有 `/storage/emulated/0/nodexq/pic/<皮肤名>/` 中存在用户新增皮肤后，列表才会出现对应的新选项，文件夹名直接作为菜单中的皮肤名称。
- **默认皮肤恢复内置并保持新命名**：修正上一版把默认棋盘/棋子移到 `app/src` 外导致编译后 APK 不含默认皮肤的问题。默认资源现放回 `app/src/main/res/drawable-nodpi/`，仍采用 `board` 与 `br/bn/bb/ba/bk/bc/bp/rr/rn/rb/ra/rk/rc/rp` 新命名；外部 `pic` 目录只用于用户新增皮肤，外部 `default` 文件夹不会覆盖内置默认皮肤。编辑局面的棋子按钮也会在外部皮肤不可用时自动使用内置棋子图。
- **皮肤动态拖动校准**：取消“一次性依次点击四个点”的校准方式。开启动态校准后，预览棋盘会叠加完整 9×10 落点网格和四个黄色角点控制柄；四个角点均可反复拖动微调，拖动网格内部可整体平移，棋子落点实时跟随变化，用户可持续调整直到满意再保存。每个皮肤仍独立保存校准值，并保留一键恢复默认位置。
- **引擎设置中文化**：Threads→线程数、Hash→哈希值、Clear Hash→清理哈希、MultiPV→主变数量、Mate Threat Depth→中规判断“杀”回合数、Repetition Rule→棋规、Asian Rule→亚规、Chinese Rule→简易中规、SkyRule→天规、Computer Rule→象棋程序竞赛规则、YitianRule→弈天规则、AllowChase→允许长捉、NoJudgement→无规则、DrawRule→和棋棋规、DrawAsBlackWin→和棋黑胜、DrawAsRedWin→和棋红胜、DrawRepAsBlackWin/DrawRepAsRedWin→和棋循环黑胜、Sixty Move Rule→自然限招、Rule60MaxPly→自然限招步数、ScoreType→显示分数格式、EvalFile→权重路径；`LUOutput` 直接隐藏。组合框仅中文显示，实际下发给引擎仍保留原始 UCI 枚举值。
- 版本升级为 V18.9（versionCode 42 / versionName 18.9）。

## V18.8 — 2026-08-15

- 补充修正对局报告错招列表：增加"回合数 / 错误招法"表头，固定列宽、统一对齐并加入行分隔；点击错招行会关闭列表并直接跳转对应棋步。
- 错招列表排版收紧：表头与数据行高 dp(38)/dp(42)→dp(30)/dp(32)，cell 上下 padding 0→dp(4)；表头"错误招法"对齐数据行改为 `Gravity.START`，与招法列左对齐视觉一致。
- 错题推演改为**棋盘实际翻转**：仅在打开时按记录的走棋方翻转一次——红方走不翻转（红方在下），黑方走翻转（黑方在下）；之后走棋、前后导航、跳首尾均保持棋盘方向固定（避免每步翻转无法下棋）。移除原"当前走棋方：红方/黑方"文字提示。
- 修复错题推演导航键动态启用后仍保持灰色外观的问题：按钮可用状态、文字颜色与背景样式现在同步刷新。
- 将错题本“错”按钮从“最近对局”标题栏迁移到“局势图”页面“报告”按钮旁；重新打分期间与报告入口一起临时隐藏，打分结束后自动恢复。
- “最近对局”标题栏恢复左右对称布局，标题重新严格居中。错题本由局势图进入后，返回层级调整为“错题推演 → 错题本 → 原局势图”。
- 重做错题本列表页布局，沿用“最近对局”的深色标题栏与列表留白；标题居中，新增顶部“清空”和每条右侧“×”删除功能。
- 临时推演与错题推演的引擎分析改为可切换开/关；分析开启时走棋、前后导航或跳首尾会自动停止旧局面并按新 FEN 续算，无需再次点击分析。
- 修正临时分析 PV 翻译引用可变棋盘导致走棋后只显示原始 UCI 招法的问题；改用分析启动瞬间的棋盘快照翻译，并继续使用“引擎设置”中的引擎与 UCI 参数。
- 临时棋盘分析箭头跟随全局箭头显示与步数设置；错题推演走棋后四个导航键会立即按当前 ply/线路长度更新。
- 菜单中的箭头显示按钮固定只占半行宽度，另一半保留空位，不再单独横跨整行。
- 单 PV 分析时隐藏箭头上的序号“1”，只显示纯箭头；MultiPV 继续显示原有 PV 编号。
- 版本升级为 V18.8（versionCode 41 / versionName 18.8）。

## V18.7 — 2026-08-15

- 对弈模式临时推演取消普通选子“已选中：x”Toast；推演底部新增与退出按钮等宽的放大镜分析入口，并使用“引擎设置”的引擎/UCI参数显示同款分析结果。
- 二合一模式 MultiPV 超过 4 时改为免确认 Toast 提示，不再阻断设置或模式切换。
- 评分报告中有错招时“错误招法”增加下划线点击入口，可查看该方错招回合与中文着法并跳转对应棋步。
- 正式对局结束后允许回溯并继续形成独立的赛后试走线路，电脑按当前难度正常应招；该线路不重复计战绩、不写最近对局，提和/认输/悔棋保持禁用。
- 删除分支后对剩余 A/B/C… 标签按顺序重新编号，修复出现 A/C/E 等编号空洞的问题。
- “显示箭头”控制从引擎/二合一内容区移入菜单，功能逻辑保持不变。
- 新增错题本：创建 `/storage/emulated/0/nodexq/correction`；棋盘长按可将当前 FEN 命名加入错题本，TXT 以确认时年月日时分秒命名；最近对局标题栏新增“错”按钮，错题可进入独立临时棋盘并分析，不影响正式棋盘。
- 版本升级为 V18.7（versionCode 40 / versionName 18.7）。

## V18.6 — 2026-08-14

### 当前 OK 状态（2026-08-15 定稿）

**分支角标（`CornerBadgeMoveView extends TextView`）**
- 角标 = 分支总数+当前线路字母（如 `2C`），在 `onDraw` 中用独立 8sp bold Paint `canvas.drawText` 绘制，**完全不参与 layout/measure**——不挡字、不占列宽、不挤引擎；
- 角标起点固定 view 右缘（`getWidth() - badgeWidth - 4dp`），所有步框横坐标统一，文字再长也不会跑到 view 外；
- 颜色：选中态 `Color.argb(230,226,240,228)` 浅白绿（深绿底上清晰），未选中 `rgb(42,92,70)` 深绿；
- 分支数 = 替代线路数 + 当前激活线路（A/B 两条分支正确显示 2A/2B）。

**二合一布局**（`棋谱+引擎 | 局势图` 两标签）
- 棋谱列：**WRAP_CONTENT**（每个步框 = 自身文字宽度，"1." 到 "1000." 有多少占多少，零多余空白）；
- 分支列：固定 = 屏幕宽 1/4，不随棋谱列变；棋谱列拓宽时整体右移（可越过中线一点）；
- 引擎列：weight 1f 吃剩余；长文字由链上容器 `setClipChildren(false)` 溢出到相邻列上方 1~2dp 兜底。

**非二合一布局**（棋谱 tab）
- 注释/分支列固定 `dp(160)`，与页内导航键等宽（导航键 MATCH_PARENT 跟随），三页面（棋谱/引擎/局势图）导航尺寸基本一致；
- 棋谱列宽度上限 = 容器可用宽（屏幕宽 − gameContentHost 左右 padding 16dp）− 分支列 160dp − 余量，窄屏不挤出屏幕。

**箭头颜色（ChessBoardView）**
- 按着法实际行棋方着色：红方箭杆红 `rgb(214,40,40)`、黑方蓝 `rgb(40,100,214)`；第 3/4 步续着用浅色版（浅红/浅蓝）；合法走法提示统一用当前行棋方颜色。判定完全由棋盘当前行棋方 + 箭头 index 奇偶推导，MultiPV 每 PV 前两步同样正确。

**其余保留**：二合一分支滚动保持。

### 用户自制改动（原文保留）
- **XQF 高版本棋子位置兼容修复**：对比节点象棋、P象棋与红鲨三份实际 XQF 样本后，确认旧实现把"零密钥"误等同于"无需高版本槽位重排"。XQF `version > 10` 时，即使 `KeyXY=0`，头部 `0x10~0x2F` 的 32 个棋子位置仍需按 `(KeyXY+i+1)&31` 做槽位轮转；旧节点 v18 导出却直接写逻辑槽位，读取时又跳过轮转，形成"自己保存自己能读、其他标准程序互读错位"的闭环。V18.6 导出继续使用零密钥 v18，但按标准高版本规则写入 32 个位置槽位，与红鲨样本的布局一致；P象棋 v10 的低版本原始槽位规则保持不变。
- **旧节点 XQF 向后兼容**：读取 zero-key v18 文件时同时生成标准高版本候选和 V18.5 及更早节点象棋的历史 raw 候选；优先用首着合法性判断，无法区分时再用帅/将、士、象的固定合法活动区域评分选择，既能正确打开红鲨等标准 v18 文件，也尽量保证以前由节点象棋错误格式保存的旧 XQF 仍可正常读取。

### 踩坑总结（本轮血泪教训，改布局必读）

1. **角标不要占 layout 空间**：FrameLayout 叠加（文字溢出被压）、LinearLayout 占位列（挤窄邻居）、padding 预留切割（中文末字 ink 被截）三轮方案全失败；**唯一正确做法 = 自定义 TextView 在 onDraw 里画**，零布局参与。
2. **"固定宽不行" = 要 WRAP_CONTENT**：列宽诉求必须以内容自适应为最小实现，宁小勿大；"够显示 100.兵三进一 就行"字面执行，不要拍脑袋加 60dp/150dp。
3. **dp 数值与 dp() 返回值（px）绝不能混加**：`Math.round(screenWidthDp*0.5f) + dp(60)` 把 dp 和 px 相加，棋谱列被挤成细条。布局宽度计算要么全 dp 要么全 px。
4. **嵌套 LinearLayout 的 weight 会层层均分**：父 weight 1f + 子 weight 1f = 子被父剩余再均分；必须有固定宽度的子要显式设固定宽。
5. **clipChildren(false) 让布局溢出可见**：为长文字溢出放开裁剪后，原本被裁剪的"总宽超容器"问题暴露成可见 bug；计算内容自适应元素上限必须用**实际容器可用宽**（扣父 padding），不是屏幕宽。
6. **布局目标不明确必须先问**：用户给出截图/描述时，先 AskUserQuestion 确认"各占屏幕一半""分支列 = 导航键等宽"这类语义，再动手——闷头猜 3 轮全错。

### 版本同步
- 基于 V18.5 源码修改，保留既有改动；应用显示 V18.6，Gradle `versionCode` 39、`versionName` 18.6；本次由用户自行编译。

## V18.5 — 2026-08-13

- **冻结范围收窄**：引擎内容快照冻结适用范围收窄——仅在"停止分析后导航/切页/走子"场景保持冻结；**新建对局、退回初始界面后重新进入（含恢复留存对局、重新进入分析模式）时立即解除冻结**，不再沿用旧局面的引擎快照，其余行为照旧。
- **停止分析后引擎内容冻结修复**：MultiPV（同时兼容单 PV）点击放大镜停止分析后，立即冻结停止瞬间的完整引擎显示快照；此后点击四个导航键、切换棋谱/局势图/引擎页、走子或其他会重建当前局面的操作，都只重置分析工作状态，不再清空、回退或重新组织已显示的分析内容。只有下一次真正启动分析时才解除冻结并允许新结果刷新。
- **MultiPV 切页/导航保持修复**：将“用户配置的 MultiPV 数”与“当前收到的 info multipv 序号”分离保存；棋谱、局势图、引擎页切换以及四个导航键引发界面重建/重新搜索时，不再因公共深度暂时缺失而退回单 PV。新局面首帧尚未返回时先保留多 PV 骨架，各 PV 到达后再原位刷新。
- **局势图实时评分 131 + ComputerRule 修正**：对弈局势图实时评分继续严格使用原有虚拟引擎槽 `131`，保持 `Threads=4`、`MultiPV=1`、`UCI_ShowWDL=true` 等原配置不变，仅为该评分实例额外固定 `Repetition Rule=ComputerRule`；不改为通用/外部引擎，也不影响分析、重新打分、提和及正式对弈引擎配置。
- **错招标记改为红黑圆圈**：移除原有“错”文字和小箭头，在错误招法对应的局势点直接画空心圆；红方失误（红方视角分数下降、折线向屏幕下方）使用红圈，黑方失误使用黑圈。第一步使用真实初始局面评分判断方向，避免首着颜色误判；本次修正将圆圈半径由 `6dp` 微调为 `5.5dp`，减少对折线和相邻点的遮挡。
- **注释框自适应高度**：移除棋谱页已有注释框的固定最大行数限制，让文本区域随棋盘缩小后释放出的可用高度自动扩展；只有内容实际超过可用空间时才在注释框内滚动，不再提前留下大块底部空白。
- **残局大漏跨阶段兜底**：保持开局/中局/残局 `0.23567 / 0.76047 / 0.01906` 的阶段权重不变，继续防止通过大量残局好棋“刷分”；对未命中 5500 超高普通分豁免且 `L>1000` 的严重大漏，额外记录该方最差单步质量分 `S`，并将阶段加权总评乘以 `0.70 + 0.30×S/100`。严重大漏无论发生在哪个阶段都会进入总评，避免残局一次极大失误仍接近 99 分。
- **声音状态按钮**：菜单新增 `当前声音:开/关`，点击循环切换走棋/将军音效并持久化；点击后菜单保持打开，不自动返回棋盘。
- **棋谱引擎二合一**：菜单在声音按钮之后新增 `棋谱引擎二合一:开/关`。开启后底部三页合并为“棋谱+引擎 / 局势图”两页；棋谱改为单列逐步纵排，原第二列改放固定 3.2 行高度的分支框与自适应注释框；四个导航键位置和尺寸保持原逻辑，其下方显示精简引擎分析，只保留深度、己分、时间、NPS，走法仍按四字模块换行。MultiPV 继续等宽分列；二合一状态下将 PV 设置为大于 4，或在 PV>4 时开启二合一，会提示“二合一之后还选这么多PV，估计没位置显示了。”并提供确定/取消。
- **对弈临时“推”演**：对弈模式顶部按钮紧凑等分并在最右增加“推”。点击后按当前 FEN 建立完全独立的临时棋盘，提供更大的四个导航键和“退出推演”按钮；临时走子、回退与前进均不写入正式棋谱、评分或对局状态。分析模式隐藏“推”，保留原 6 个顶部按钮。
- **引擎招法红黑着色**：无论普通布局或二合一、单 PV 或多 PV，引擎分析中的红方招法使用红色字体，黑方招法保持黑色；四字招法模块的不可断行逻辑保持不变。
- **完整对弈报告置灰修复**：正常对弈开局后主动评估初始局面；终局后顺序补齐可能因搜索 stop/覆盖而缺失的固定 100ms 局势评分，解决棋已完整结束但因初始分或个别 `scoreKnown` 缺失导致“报告”长期灰色的问题。
- **箭头步数循环开关**：单 PV 下改为 `箭头显示1步 → 2步 → 3步 → 4步 → 不显示箭头 → 1步` 循环；第 1～4 步依次表示行棋方第一步、对方第一步、行棋方第二步、对方第二步，第 3/4 步使用相同且更浅的续着箭头色。MultiPV 仍只提供“显示箭头/不显示箭头”，并保持原有每个 PV 前两步箭头逻辑。二合一状态下开关放在初始局面/棋谱与导航区域之间的空白位置。
- **设置持久化与版本同步**：声音、二合一模式、箭头步数均写入本地偏好并兼容旧版箭头布尔设置；应用显示版本升级为 V18.5，Gradle `versionCode` 升级为 38、`versionName` 升级为 18.5。《打分算法.txt》同步记录 V18.5 的 `τ=15` 默认值和严重大漏兜底公式。

## V18.4 — 2026-08-11

- **局势图标注互不遮挡修正**：撤除“红优/黑优/绝杀”“错”和小箭头的白色底衬/白色描边，不再用覆盖曲线的方式保证可读性；标注改为从多组上下、左右、斜向候选位置中动态选择，优先避开红蓝局势曲线和已放置文字。“错”使用短斜向箭头，并在箭头尖端与目标点之间保留小间隙，尽量让文字、箭头和局势曲线三者同时完整可见。
- **超高普通分错招豁免**：当走前、走后都没有 Mate 信息，且两端均处于同一优势方向的 `|普通分| > 5500` 区间时，即使 `L > 1000` 也不计入错误招法数量、不显示“错”和小箭头；跨越 5500 阈值、优势方向翻转或任一端进入 Mate 时仍正常判错。该规则只影响错招统计/标注，不影响单步百分制分和阶段评分。
- **报告精确权重修正**：开局/中局/残局权重更新为 `0.23567 / 0.76047 / 0.01906`。三项之和为 `1.01520`，总评继续按实际存在阶段的权重和归一化，保证满分为 100，并兼容缺少某阶段的短棋谱。
- **Mate 等效分修正**：统一为 `max(0, 9000 - (x-1)×300)`，即 M1=9000、M30=300、M31 及以上=0；随项目附带的《打分算法.txt》同步修正文档说明。
- **Mate 退步折扣重排**：按近杀处罚更重、远杀更宽容的直觉调整为 M1=`0.9`、M2-M3=`0.7`、M4-M6=`0.5`、M7-M10=`0.35`、M10以上=`0.2`。
- **阶段评分改用 SoftMin**：开局/中局/残局不再取单步分算术平均，改为 `-τ·ln(average(exp(-score_i/τ)))`；V18.4 暂定 `τ=20`，减少大量高分招法对个别严重失误的掩盖。`PHASE_SOFTMIN_TAU` 独立为常量，后续可直接调参。
- **SoftMin τ 用户可调**：对局报告弹窗新增 τ 输入框（默认 15），填任意大于 0 的值后点“重算”，报告总评/分阶段分/错招数立即按新 τ 重新计算；顶部提示同步显示当前 τ，输入框下方注明“参数越大打分越宽松，越小打分越严格，默认值15”。算法默认常量同步为 15，报告页面新增注释“每步打分时间越久越准确”。
- **分支框高度微调**：分支框固定预留高度由 3 行调整为 3.2 行（`BRANCH_ROW_HEIGHT_DP × 3.2`），略露出第 4 行的一截，提示下方还有更多分支可滚动查看。
- **算法文档入包**：项目根目录新增并同步更新《打分算法.txt》，记录 5500 超高分豁免、精确权重、Mate 换算/折扣、SoftMin 聚合及缺阶段归一化规则。
- **版本同步**：应用显示版本升级为 V18.4，Gradle `versionCode` 升级为 37、`versionName` 升级为 18.4。

## V18.3 — 2026-08-11

- **新增百分制对局报告**：局势图页新增“报告”按钮；报告按红黑双方分别显示总评、开局/中局/残局百分制评分和错误招法数量。单步质量以走前最佳评估与实际走后评估的损失 L 为基础，按随附《打分算法》分段映射到 0～100 分。
- **评分完整性独立记录**：新增 `scoreKnown` 与初始局面评分状态，不再用数值 0 判断“是否有评分”，因此真实的均势 0 分不会被误判为缺失。旧 V18.2 留存因没有完整性字段默认视为未完整评估，重新打分后即可生成报告。
- **完整报告门槛**：报告要求初始局面及棋谱每一手走后的局面均存在真实评分；有任一局面缺失时，“报告”按钮置灰不可点击，同时不绘制错招文字和箭头。
- **重新打分补评初始局面**：当重新打分范围从第 1 手开始时，先以相同 `movetime` 额外评估一次初始局面，为第一手的“走前最佳评估”提供基准；随后再按所选方向逐手重新打分。
- **错招图示**：完整报告中，损失 `L > 1000` 的招法在局势图对应点旁显示小字“错”，并以小箭头指向该招；红黑双方报告分别统计自己的错误招法数量。
- **报告随分析实时更新**：用户在任意棋谱局面开启分析后，主 PV 的最新评分会继续覆盖该局面评分，并即时重新计算报告、错误招法数量和局势图错招标记；报告弹窗打开期间也会同步刷新。
- **阶段划分对齐算法**：开局固定为第 1～10 回合；残局条件改为“红黑双方各自剩余车/马/炮数量均 ≤ 2”，从首次满足条件的回合起进入残局，中间部分计为中局。
- **权重边界处理**：算法给出的 0.236 / 0.761 / 0.019 三项权重总和为 1.016。实现按这些权重的相对比例做归一化加权平均，保证全阶段 100 分时总评仍为 100；短棋谱缺少某阶段时，仅用实际存在阶段的权重归一化。
- **局势图控制栏微调**：“重新打分”按钮由 76dp 缩至 66dp，并在其右侧增加 48dp 的“报告”按钮。
- **会话/分支持久化同步**：评分完整性随主线和分支一起保存、恢复和切换；会话 schema 升级为 5，避免分支切换后报告错误地继承或丢失完整性状态。
- **版本同步**：应用显示版本升级为 V18.3，Gradle `versionCode` 升级为 36、`versionName` 升级为 18.3。

## V18.2 — 2026-08-10

- **MultiPV 自适应等宽布局**：移除 MultiPV 的固定 `108dp` 列宽和横向 `HorizontalScrollView`；同一深度下有 N 个 PV 时，N 列在当前可用屏幕宽度内按相同权重平均分配，不再把 PV4/PV5 等推到屏幕外，也不再出现横向滚动条。
- **引擎走法按整着紧凑换行**：单 PV 与 MultiPV 都优先把“炮八平五”“马２进３”等一整着视为不可拆分单位，着与着之间只保留一个紧凑空格；每行按实际宽度尽量容纳最多的完整着法，放不下的整着整体移到下一行。MultiPV 极端窄列若连一整着都无法容纳，则按字符普通换行，避免截断或横向溢出。
- **重新打分弹窗对齐**：三行参数统一固定左侧标签宽度、控件高度和起始位置；“范围(回合)”的起止输入框等宽，“打分方向”的两个单选项等宽，说明文字与输入区左边界保持一致。三行标签由右对齐改为左对齐。
- **打分方向互斥修复**：修复“从前往后 / 从后往前”两个选项可同时勾选、方向判定不生效的问题——单选按钮先加入 `RadioGroup` 再设置初始选中（此前在加入前 `setChecked(true)`，导致组内互斥状态机失效，两个选项可同时勾选且反向永不生效）；确认时按 `RadioGroup` 当前选中项判定方向。
- **打分方向单行显示**：“从前往后 / 从后往前”两个单选按钮强制单行显示（`setSingleLine` + 清除系统默认 `minWidth`），不再因列宽不足折成两行。
- **引擎设置隐藏默认值注释**：引擎设置选项行不再显示“默认：xxx”注释（如 EvalFile 的“默认：pikafish.nnue”），仅显示选项名。
- **关于应用新增 QQ 群**：作者行下方新增“QQ交流群：511645431”。
- **对弈模式禁用打开文件**：菜单“打开文件”在对弈模式下置灰不可点击（打开文件会污染对局棋谱），分析/自主分析模式正常可用。
- **重新打分说明左对齐**：弹窗底部“留空表示……”灰色说明文字不再与参数列对齐，改为从最左边界开始显示。
- **每步用时点击即全选**：每步用时及“范围”的起止输入框改为点击即全选（`setSelectAllOnFocus(true)`，与引擎设置输入框一致），不再需要先点一次聚焦、再点一次才全选；直接输入即可覆盖原值。
- **版本同步**：应用显示版本升级为 V18.2，Gradle `versionCode` 升级为 35、`versionName` 升级为 18.2。

## V18.1 — 2026-08-10

- **移除 CBR / CBL**：撤回 V18.0 新增的 CCBridge 棋谱支持，删除对应解析/保存模块、文件选择器 MIME、外部打开扩展名关联和保存格式选项；现阶段“打开/保存棋谱”只保留 XQF 与 PGN，避免不可用格式干扰正常功能。
- **保存扩展名修复**：主动保存 PGN/XQF 时分别使用 `application/x-chess-pgn` 与 `application/x-xqf`，并由系统创建 `.pgn` / `.xqf` 文件，避免文档提供器按 `text/plain` / `application/octet-stream` 自动改成 `.txt` / `.bin`。
- **棋谱分支角标修正**：步框右上角不再显示“总分支数量对应字母”，改为显示当前正在浏览的活动线路字母；例如同一节点有 A/B/C 三条线路，当前浏览 B 时显示 B，无分支时不显示。
- **重新打分方向**：参数弹窗第三行新增“打分方向：从前往后 / 从后往前”，默认从前往后；反向模式按所选范围从末端向前逐步打分。每步用时仍默认 `0.1` 秒并在打开弹窗时自动全选，删除多余的清空交叉按钮。
- **己方视角评分**：引擎分析统一显示“己分”，以当前棋盘下方一方为正负视角；红在下方时显示红方视角，黑在下方时自动反号。翻转棋盘后显示会立即同步切换；WDL 也按下方一方视角同步交换胜/负。
- **MultiPV 布局重做**：PV 数量大于 1 时按“深度模块”显示。模块首行显示深度、时间、NPS、节点数、HF；下方各 PV 横向分列，直接用蓝色阿拉伯数字编号，随后显示“己分 / WDL”。己分大于等于 0 使用蓝色，小于 0 使用红色，走法保持黑色；PV 列之间加入竖向虚线，不同深度模块之间加入横向虚线。
- **包名整理**：源码 Java 包/目录从 `com.tyl.xiangqi.qsll` 调整为 `com.tyl.xiangqi.ndxq`；保留 `core` 这一层用于规则、着法和 XQF/PGN 解析，当前已是扁平职责分层，不再继续压平。应用 `applicationId` 保持 `com.tyl.xiangqi.nodechess`，避免无必要改变安装身份。
- **MainActivity 继续瘦身**：把 XQF/PGN 文件选择器、MIME、URI 读写与文本解码移入 `storage/ManualFileIo`，把 MultiPV 模块视图和通用分析数字格式化移入 `ui/EngineAnalysisPanel`；在增加本轮功能后 `MainActivity.java` 由 V18.0 的约 6060 行进一步降至约 5990 行。
- **版本同步**：应用显示版本升级为 V18.1，Gradle `versionCode` 升级为 34、`versionName` 升级为 18.1。

## V18.0 — 2026-08-10

- **最近对局删除管理**：最近对局页右上角新增“清空”，执行前提示“是否清空所有对局记录？”；每条记录最右侧新增灰底白色 × 删除键，执行前提示“是否删除本条对局记录？”。确认后同步删除 `/storage/emulated/0/nodexq/recent` 中对应 XQF 文件，清空会逐个删除全部最近对局文件。
- **棋谱步框分支角标**：棋谱中存在多线路的着法在步框右上角显示线路总数对应字母（2/3/4 条显示 B/C/D……，例如 3 条显示 C）；无分支不显示。每回合第一步框额外加宽，给右上角字母预留空间。
- **CBR / CBL 棋谱支持**：新增独立 `CbrCblManualUtils.java`，按随附 CCBridge 数据结构实现 CBR 初始局面、主线着法、逐着注解、结果字段的读取/保存；CBL 可扫描并选择库内棋谱，保存支持单谱 CBL 容器。文件选择器、保存格式和外部打开关联同步加入 `.cbr/.cbl`。CBL 未公开摘要字段按已知固定值/零值处理，不猜测未知业务字段。
- **棋盘坐标按模式显示**：对弈模式与自主分析模式隐藏棋盘“一二三四五六七八九 / 123456789”位置标识；进入局面编辑时临时显示，退出编辑后再次隐藏，用于核对红帅、黑将方向。
- **顶部工具栏配色**：分析/对弈页顶部按钮及其所在行改为与初始页一致的浅色背景；按钮按下时显示深绿色反馈。放大镜在分析开启期间持续保持深色，关闭分析后恢复普通浅色状态。
- **重新打分参数扩展**：每步用时默认 `0.1` 秒，右侧新增灰色 × 可一键清空；新增“范围(回合)：起 至 止”。只填起点则打分到最后回合，只填终点则从开局开始，两项留空则全部回合；进度按本次选择范围重新计数。
- **引擎设置作用范围提示**：设置页顶部新增“此设置仅影响局面分析与重新打分”，明确不影响正式对弈难度配置。
- **引擎默认值与恢复默认**：引擎设置行显示引擎声明的默认值，并新增“恢复默认”按钮；恢复时清除 `manual_uci::` 手动覆盖并重新应用 UCI 默认值，避免误改 `EvalFile` 等参数后持续加载失败。
- **代码分层检查**：保留 `com/tyl/xiangqi/qsll/core` 这一标准 Java 包层级及规则/记谱独立模块；该目录职责清晰，无必要压平。新增 `storage/RecentGameStore` 与若干 `ui` 对话框/页面构建器，把最近对局文件管理、最近对局页面、保存格式弹窗、重新打分弹窗从 `MainActivity` 拆出。
- **MainActivity 初步瘦身**：在增加本轮功能的同时，`MainActivity.java` 从 V17.8 的 6088 行降到约 6060 行；新增的 CBR/CBL 编解码、最近对局文件层和多个纯 UI 组件均保持在独立类中，便于后续继续拆分。
- **版本同步**：应用显示版本升级为 V18.0，Gradle `versionCode` 升级为 33、`versionName` 升级为 18.0。

## V17.8 — 2026-08-06

- **公共目录与最近对局**：首次启动时申请公共存储访问权限，并创建 `/storage/emulated/0/nodexq/recent`。正式对弈完成后自动按 XQF 格式保存棋谱；局势图评分不写入 XQF。
- **首页最近对局入口**：在“分析模式”和“设置”之间新增“最近对局”。列表仅展示已完成对弈，按“红方名称 先胜/先和/先负 黑方名称  YYMMDDHHMM”显示。
- **最近对局打开规则**：点击记录后进入分析模式；分析模式为空时直接加载，已有棋谱时提示“分析模式已有棋谱，是否覆盖？”。在分析模式中的编辑只修改当前分析副本，不影响独立存储的历史战绩统计。
- **分析转对弈保护**：从分析模式进入对弈模式前检查对弈模式是否存在未完成对局；存在时提示是否覆盖，不存在时直接进入。分支保留规则继续沿用原有确认流程。
- **终局返回简化**：对弈完成后按系统返回键直接回到初始界面，不再提示“确定返回初始界面吗……”，已完成棋谱由最近对局自动保存。
- **模式入口调整**：“进入对弈模式 / 进入分析模式”移入棋盘页菜单。分析模式下棋盘上下方不再显示“红方/黑方 · 自主分析”，对弈模式玩家信息保持不变。
- **返回初始界面免确认**：从分析/对弈进行中按系统返回键时，不再弹出“确定返回初始界面吗？”，默认保留当前棋谱直接返回初始界面。
- **最近对局列表优化**：列表时间戳显示补全到秒（与文件命名 `yyMMddHHmmss` 一致，避免同分钟对局显示混淆），并在日期与时刻之间加逗号分隔（如 `260806,151222`）；结果按"先胜（红） / 先负（灰） / 先和（默认色）"着色显示，胜负结果一目了然。
- **引擎页导航栏对齐**：引擎页顶部的 4 个导航按钮（最前 / 前一步 / 后一步 / 最后）原先撑满整屏宽度，改为与棋谱页分支区导航按钮一致的宽度（左侧让出棋谱栏宽度的 spacer），三页导航按钮尺寸统一。
- **引擎设置精简**：隐藏无用的 `Skill Level`、`UCI Limit Strength`、`UCI ELO` 三个强度控制选项（难度由档位决定，不依赖这些选项）；引擎设置对话框整体收紧——面板内边距、行间距、选项行高与控件高度均减小，显示更紧凑。
- **分析模式上下行隐藏**：分析模式下棋盘上方“红方/黑方 · 自主分析”与下方玩家标签两行已无文字，直接整行隐藏不占位，棋盘区域更紧凑；对弈模式仍显示玩家信息行，编辑模式的独立控制不变。棋谱区高度改为按上下行实际可见性计算，并在模式切换时自动重算，避免隐藏两行后棋谱导航与注释框底部留白。
- **恢复棋盘箭头功能**：恢复引擎页“棋盘显示箭头”勾选框及棋盘分析箭头绘制（从 v17.7 补回，含 `ThoughtArrowRenderer` 统一绘制器）；分析箭头随引擎 PV 实时更新，箭头开关状态持久化保存。
- **难度调整**：“市县好手”的搜索预算从 10500 节点提升为 `go nodes 15000`。
- **版本同步**：应用显示版本升级为 V17.8，Gradle `versionCode` 升级为 32、`versionName` 升级为 17.8。

## V17.7 — 2026-07-31

- **走棋音效**：加入源码自带的 `move.wav` 与 `check.wav`。正常走子、吃子以及棋谱向“后”导航时播放音效；形成将军时优先播放将军音效；向“前”回退导航时保持静音。
- **已有分支复用**：从历史局面走子时先按当前节点和落子坐标查找已有分支；命中后直接切换并导航到该线路，不再重复创建首招相同的分支，只有不存在对应分支时才建立新线路。
- **分支与注释区精简**：移除“初始局面的分支 / 第 x 步的分支”标题框，也移除注释区的步骤标题；空注释仅在中央显示灰色“注释框”占位文字。
- **分支与注释高度调整**：分支框固定预留三行分支的高度，超过三行时在框内滚动；注释框始终占据该三行分支区域下方的全部剩余空间，不随实际分支数量变化。
- **局势图超限分数修复**：普通引擎评分超过 6000 时保留并显示真实分数，不再因为绘图坐标截断而误显示为“红方胜 / 黑方胜”；规则终局补分 ±6000 仍按胜负显示。
- **版本同步**：应用显示版本升级为 V17.7，Gradle `versionCode` 升级为 31、`versionName` 升级为 17.7。

## V17.6 — 2026-07-31

- **难度调整**：街头霸王搜索预算从 1000 节点提升至 1700 节点，招法质量小幅增强。
- **新增难度"市县好手"**：使用 duf 引擎，脱库 8 回合，2 线程，每步限 10500 节点；难度介于县镇好手（4600 节点）和业余顶尖（25000 节点）之间。
- **版本同步**：应用显示版本升级为 V17.6，Gradle `versionCode` 升级为 30、`versionName` 升级为 17.6。

## V17.5 — 2026-07-23

- **立即出招取消固定门槛**：移除 V17.4 的“至少 180ms 且深度 15、最长 700ms”等待。用户点击闪电按钮后立即向当前 `go infinite` 搜索发送一次 `stop`，不再额外等待固定时间或深度；仍严格只执行同一局面返回的最终 `bestmove`，不会采用中途 PV、猜测着法或第一步合法着法。
- **连续局面搜索空窗缩短**：收到 `bestmove` 并落子后，下一局面的分析从延迟 20ms 改为立即安排；正常的 `bestmove → position → go infinite` 切换不再每步额外发送 `isready`，只在新局、引擎启动或选项变更等真正需要同步的场景等待 `readyok`，使用户两次快速点击之间的时间尽量全部用于搜索。
- **避免重复加载 NNUE**：相同 `EvalFile` 已经成功下发后不再在每个局面重复发送 `setoption`。兼容会在重复设置时重新加载网络的引擎，减少无效初始化和首层输出延迟。
- **分析 UI 合帧**：引擎密集输出的 `info` 仍全部保留到深度/多主变列表，但改为每 16ms 批量进入主线程并统一刷新箭头、文字和局势图，避免大量细碎 `Handler` 任务阻塞闪电按钮和 `bestmove` 回调。最终 `bestmove` 回调保持即时处理。
- **菜单扁平化**：去掉"设置"子菜单，原嵌套的"日志""引擎设置""棋盘大小"直接平铺到主菜单的"保存棋谱"之后，减少一次点击。
- **版本同步**：应用显示版本升级为 V17.5，Gradle `versionCode` 升级为 29、`versionName` 升级为 17.5。

## V17.4 — 2026-07-23

- **立即出招质量修复**：对比“立即出招”和“重新打分”的通信链路后确认，V17.3 会在已有任意 `info pv` 时直接采用 PV 第一着，甚至 `stop` 后尚未返回 `bestmove` 前到达的浅层 PV 也可能触发落子；快速连续点击时常只搜索到深度 1～数层，因此招法质量明显下降。V17.4 禁止使用中途 PV 直接落子，改为复用当前 `go infinite` 搜索，至少运行 180ms 且达到深度 15 后才发送一次 `stop`；慢设备最迟在 700ms 收束，随后只接受当前局面最终返回的 `bestmove`。已经分析充分的局面仍可立即 `stop`，兼容循环有限时搜索的引擎也能正确把主动停止返回给界面，不会误开下一轮搜索。
- **与重新打分统一最终结果语义**：重新打分之所以稳定，是因为每个局面都按固定 `movetime` 完整搜索并等待 `bestmove`，分数取搜索结束前最后一条有效 `info score`。立即出招现在采用相同的“中途信息只显示、最终 `bestmove` 才执行”原则，同时继续利用实时分析已经积累的置换表和搜索时间，避免重新启动独立搜索。
- **菜单按钮位置调整**：棋盘页“菜单”按钮移动到“新建”左侧，其余顶部按钮位置和功能保持不变。
- **菜单项目顺序调整**：菜单按“提和、认输、悔棋、编辑、打开文件、保存棋谱、设置”的顺序排列，仍一次显示全部项目且不使用滚动条。
- **版本同步**：应用显示版本升级为 V17.4，Gradle `versionCode` 升级为 28、`versionName` 升级为 17.4。

## V17.3 — 2026-07-23

- **顶部工具栏改为单排菜单布局**：新增自绘“菜单”图标，将原第二排的“打开、存储、提和、认输、悔棋、设置”以及第一排的“编辑”移入菜单；菜单使用四行双列文字按钮，一次显示全部七项且不使用滚动条。原按钮调用的方法、启用条件和业务逻辑保持不变；棋盘页工具栏从 66dp 缩至 34dp，棋盘整体上移以节省屏幕空间。
- **注释区空间与空白状态调整**：棋谱页导航栏下方改为分支列表占三分之二、注释框占三分之一；空注释不再显示“点击弹窗编辑……”等提示文字，仍可点击整个注释区打开编辑窗口。
- **重新打分进度移入局势图控制栏**：重新打分期间不再显示居中 Toast，也不显示“打分中”按钮；仅保留“停止”，并在“停止”和导航按钮之间实时显示“当前步骤/总步骤、深度”。完成或中断后自动恢复“重新打分”。
- **分析转对弈不计战绩**：从首页进入分析模式后再切换到对弈模式，明确保持 `competitiveResultEligible=false`，并在战绩写入方法增加二次保护，终局不会计入历史胜和负。
- **首页精简**：移除首页“调整棋盘大小”按钮；棋盘尺寸调整只保留在棋盘页“设置 → 调整棋盘大小”中。
- **终局弹窗精简**：胜负结束弹窗移除圆形“胜”图标，胜方结果统一只显示“红方胜！”或“黑方胜！”。
- **分析与立即出招响应优化**：根据通信日志中的 `go infinite → stop → bestmove → 新 position → go infinite` 节奏，将可能等待旧搜索结束的分析启动移到专用后台线程，避免阻塞主线程；引擎实时信息改为直接更新现有文本控件，不再每次重建整个标签页；刷新节流缩短，并将重复分析重启合并。快速点击“立即出招”时，已有主变就直接采用；主变尚未到达但当前搜索已启动时立即发送 `stop` 并采用随即返回的 `bestmove`；搜索尚在启动阶段则登记一次待执行请求，在首条有效 PV 到达后立即落子，不再弹出“正在分析，请等待”提示。
- **标签页位置记忆**：棋谱、引擎、局势图的最后选择写入偏好设置；进入分析模式、对弈模式或打开棋谱时不再强制回到“棋谱”标签。
- **关于应用文字**：特别鸣谢中的“ChatGPT5.6”改为“GPT5.6”。
- **版本同步**：应用显示版本升级为 V17.3，Gradle `versionCode` 升级为 27、`versionName` 升级为 17.3。

## V17.2 — 2026-07-21

- **注释改为弹窗编辑**：棋谱页注释框不再直接唤起输入法；点击注释区后弹出较大的多行编辑窗口，输入法随弹窗自适应，保存后同步更新初始局面简介或当前步骤注释并自动留存。
- **棋盘尺寸使用真实页面预览**：删除独立缩略预览画面；“调整棋盘大小”只显示白色滑条弹窗及“取消 / 保存”。从初始页开始拖动时直接进入实际分析模式棋盘页，拖动过程中实时改变真实棋盘尺寸；取消恢复原尺寸，保存后持久化。
- **重新打分进度 Toast 复用**：整次重新打分只创建一个居中 Toast，后续仅更新其中的“当前步数 / 总步数”和深度，不再每一步或每个深度创建新 Toast；完成时统一关闭。
- **分支切换日志精简**：日志不再记录“已切换到分支 X”一类普通界面操作。
- **引擎设置选项隐藏**：引擎设置界面过滤 `Debug Log File`、`NumaPolicy`、`Ponder`、`Move Overhead`、`nodestime`，其他 UCI 选项保持原有编辑与保存逻辑。
- **大棋盘下的注释可达性**：棋盘页面支持在内容超出屏幕时纵向滚动，并为棋谱/引擎/局势图区保留最小高度；80%～100% 等较大棋盘尺寸下仍可滚动到注释框并点击编辑，同时保护棋盘、分支列表、棋谱列表和局势图自身的触控操作。
- **版本同步**：应用显示版本升级为 V17.2，Gradle `versionCode` 升级为 26、`versionName` 升级为 17.2。

## V17.1 — 2026-07-21

- **分支层级逻辑修复**：XQF 不再把某条线路后续的所有叶子分支提前展开到当前一步；分支数据改为“当前节点的直接走法 + 各线路自带的后续分叉”。当前一步只列出本步可选走法，选择线路并导航到后续分叉点时，才显示该步的分支；切换或删除线路时同步保留、交换或移除其衍生分支。
- **分支列表滚动位置保持**：点击切换分支后重建棋谱区域时恢复原分支列表滚动位置，不再自动回到最上方。
- **外部打开复用应用本体**：主 Activity 从 `singleTop` 调整为 `singleTask`，并禁用文档型多实例启动；从 QQ、文件管理器等应用打开 XQF/PGN 时复用节点象棋现有任务和本体界面，通过 `onNewIntent` 接收文件，不再额外创建一份节点象棋界面。
- **初始局面注释**：初始局面注释框改为可编辑，可用于棋谱整体简介；内容支持 XQF 根注释、PGN `#1,1#` 首注释的读取与存储，并加入自动留存、复制棋谱和恢复会话。
- **分支标题用语修正**：分支栏标题改为“第x步的分支”，例如第五回合红方着法显示“第9步的分支”；初始位置显示“初始局面的分支”。分支行只显示当前这一步的直接走法，不再把后续着法串在同一行。
- **版本同步**：应用显示版本升级为 V17.1，Gradle `versionCode` 升级为 25、`versionName` 升级为 17.1。

## V17 — 2026-07-21

- **XQF / PGN 文件读写**：移植并适配 V68 的 XQF 棋谱树形读写能力，支持从系统文件选择器打开 XQF、PGN，按 XQF 或 PGN 二选一存储到用户指定路径，并用 Toast 提示成功或失败；XQF 可往返保留主线、分支和逐着注释，PGN 可读取并保存逐着注释。
- **外部文件打开关联**：为 XQF/PGN MIME、常见二进制 MIME 及 `.xqf`/`.pgn` 文件路径注册 `VIEW` Intent，第三方文件管理器或聊天应用的“使用其他应用打开”列表可显示节点象棋；Activity 使用 `singleTop` 接收再次打开的文件。
- **棋谱分支显示语义修复**：定位到第 N 手时，分支栏改为展示包含第 N 手的当前选中分支及同一节点的其他分支，不再提前展示下一手才会出现的分支。
- **棋谱注释区**：棋谱页右侧导航栏以下区域按 75% / 25% 分为分支列表与注释框；注释框显示并可编辑当前着法的 XQF/PGN 注释，内容同步进入分支、自动留存、复制和文件导出。
- **棋盘长按剪贴板菜单**：工具栏取消“复制局面/棋谱”和“粘贴”按钮；长按棋盘弹出“复制局面 / 复制棋谱 / 粘贴”，原有功能保持不变。
- **打开与存储工具栏按钮**：在“提和”左侧加入常见文件夹“打开”图标和软盘“存储”图标；存储弹窗使用 XQF/PGN 单选圆点和“选择路径 / 取消”操作。
- **引擎箭头开关**：引擎标签页导航键左侧新增“棋盘显示箭头”勾选项，选择状态持久化并即时控制棋盘箭头显示。
- **局势图布局与进度提示**：“重新打分 / 停止”上移到与导航键同一行；逐步打分的“重新打分 a/b、深度 xx”改为棋盘中央 Toast，不再占用局势图页面空间。
- **棋盘大小实际页面预览**：“调整棋盘大小”拖动时按分析模式实际页面尺寸预览 66dp 工具栏、30dp 玩家栏、实际棋盘、30dp 玩家栏、36dp 标签栏和棋谱区，不再使用旧版缩略布局。
- **版本同步**：应用显示版本升级为 V17，Gradle `versionCode` 升级为 24、`versionName` 升级为 17。

## V16.1 — 2026-07-18

- **模式切换按钮位置纠正**：撤销 V16.0 新增的工具栏第三行。“进入对弈模式 / 进入分析模式”继续与顶部“x方 · 自主分析/玩家/电脑”处于同一行，只将按钮缩小到接近工具栏“设置”按钮的六等分宽度。
- **上下玩家文字中心对齐**：顶部玩家标签改为整行居中的覆盖布局，模式按钮固定在右侧并为文字预留对称空间；顶部与棋盘下方的“x方 · 自主分析”保持相同 x 轴中心，不再因右侧按钮而偏移。
- **duf 调用修复方向纠正**：V16.0 把 `xiangqi-agg20260512.nnue.so` 重命名为 `libxiangqi-agg20260512.nnue.so` 并强制发送绝对 `EvalFile`，日志证明文件存在且大小正确，但 Duffish 仍在首次 `go` 报“network file was not loaded successfully”并退出。进一步检查当前 `libduf.so` 后确认其已完整内嵌 `xiangqi-agg20260512.nnue`，且内嵌数据与单独网络文件逐字节一致；因此 V16.1 对 duf 清除会话、持久化及手动设置中的 EvalFile，不再自动匹配或下发外置网络路径，直接使用内嵌 NNUE。HCE、131 和 Pikafish 网络处理保持不变。
- **duf 重复网络不再打包**：Gradle 构建排除 `xiangqi-agg20260512.nnue.so` 与 `libxiangqi-agg20260512.nnue.so`，避免把已内嵌的数据再次塞入 APK，也避免 Android 原生库命名触发错误加载路径。
- **版本同步**：应用显示版本与 Gradle 版本升级为 V16.1（versionCode 23）。

## V16.0 — 2026-07-18

- **编辑局面返回键修复**：编辑局面时按手机系统返回键，不再按“空棋谱”规则返回初始界面；现在优先执行编辑面板“取消”的同一逻辑，恢复进入编辑前的局面、手数和界面状态。
- **模式切换按钮布局调整**：“进入对弈模式 / 进入分析模式”移到棋盘页工具栏“设置”按钮正下方，使用相同的六等分按钮尺寸；顶部玩家信息恢复整行宽度，使上下两侧“红/黑方 · 自主分析”的文字中心保持同一 x 轴坐标。
- **duf 少数设备首次搜索退出的真正根因修复**：V15.1-V15.3 修复了 EvalFile 的选择和下发，但源码中的 `xiangqi-agg20260512.nnue.so` 缺少 Android 原生库稳定提取所需的 `lib` 前缀，且 Gradle 只重命名了 Pikafish 网络。部分 Android 版本或安装方式没有把该文件提取到 `nativeLibraryDir`，因此 duf 可以完成 UCI 握手，却在第一条 `go` 命令加载 NNUE 时以退出码 1 结束；另一些设备会提取或残留该文件，所以表现正常。V16 构建时自动将其重命名为 `libxiangqi-agg20260512.nnue.so`，运行时同时兼容新旧名称。
- **引擎故障日志增强**：固定难度实战失败时记录 `activeEvalFile`、文件存在性与大小、实际发送的 EvalFile 命令、安装目录全部网络候选，以及引擎在 `go` 后退出前的原始输出；搜索前若网络路径已经失效，会在发送 `go` 前直接给出明确错误。
- **版本同步**：应用显示版本与 Gradle 版本升级为 V16.0。

## V15.3 — 2026-07-18

- **duf 引擎 EvalFile 未下达根因修复（第二次）**：V15.2 在 `ensureStarted` 发送链末尾加了兜底发送，但忽略了 `configureEvalFileForEngine` 内部还有另一处拦截——`selected` 为 null 时 `else` 分支对内置引擎直接移除 `sessionOptions` 中的 EvalFile，不尝试任何自动解析，导致 `activeEvalFile` 保持空、兜底发送前提条件不成立。修复：`else` 分支新增最后尝试——用 `sessionValue`（duf 为 `xiangqi-agg20260512.nnue`）或 `NNUE_FILE` 调用 `resolveEvalFileNamedByOption` 直接匹配工作目录，成功则同时更新 `sessionOptions`/`activeEvalFile`/`option.currentValue` 三条路径。

## V15.2 — 2026-07-18

- **duf 引擎 EvalFile 未下达根因修复**：V15.1 在 `configureEvalFileForEngine` 中正确将 duf 的 `xiangqi-agg20260512.nnue` 写入了 `sessionOptions`，但 `ensureStarted()` 中 session/pending 选项发送循环调用 `findParsedOptionName(e.getKey())` 查找 UCI 解析后的选项名，duf 的 UCI 输出不含 EvalFile 选项故返回 null，**该条目被跳过、`setoption name Evalfile` 从未发送**，引擎仍用内置默认路径（相对路径）加载 NNUE 失败。修复：在 session/pending 发送之后新增兜底检查——若 `lastSentEvalFileCmd` 标记为未发送但 `activeEvalFile` 已有有效路径，直接用 `"EvalFile"` 字面名发送 `optionCommand`，绕过 `findParsedOptionName` 的 null 过滤。

## V15.1 — 2026-07-18

- **duf 引擎 NNUE 误配修复**：`resolveAutomaticEvalFile` 中 `isApkNativeEngine` 对所有内置引擎（含 duf）返回 true，直接匹配 `pikafish.nnue.so` 并提前返回，跳过后续的 option/default 解析和目录扫描，导致 duf 拿到了 131 的 NNUE 文件退出。修复：(1) 该速配路径增加 `isDuf` 判断，duf 跳过 pikafish 默认名；(2) `configureGameEngine` 为 duf 显式设置 session EvalFile 为 `xiangqi-agg20260512.nnue`，经 `resolveEvalFileNamedByOption` 补齐 `.so` 后缀；(3) `configureEvalFileForEngine` 新增 `findParsedOptionName` null 回退，确保即使 UCI 输出截断也能继续下发。
- **关于应用文本更新**：致谢改为"感谢Qsll项目以及Pikafish、Duffish、PikafishHCE、Tchess这几个开源项目，为本项目提供了重要的参考和帮助。"；标题下方副标题"中国象棋·人机对弈与棋局分析"已删除。
- **开局库覆盖安装更新修复**：`installAndLoadRpBook()` 中 `if (!book.isFile())` 判断文件已存在即跳过解压，导致覆盖安装后 APK 内置新版 `book.obk` 无法替换 `getFilesDir()` 中的旧副本。修复：移除该条件判断，每次启动始终从 assets 覆盖解压最新开局库。

## V15.0 — 2026-07-18

- **覆盖安装后放大镜分析失败的真实根因**：日志证明 `lib131.so` 已成功启动并完成 UCI 握手，退出发生在 `go` 之后；引擎明确报告未能加载 `pikafish.nnue`。此前 V10/V11 将问题归因于 `ucinewgame/isready` 重置 EvalFile，但本次日志显示启动会话中 `activeEvalFile=(未设置)`，且实际未下发有效的绝对路径，因此原补发逻辑没有解决根因。
- **修复会话参数反向覆盖**：引擎设置保存的 `manual_uci::EvalFile=pikafish.nnue` 原先会在自动解析之后再次作为 session option 发送，覆盖可能已解析出的绝对路径；V15 在握手后统一解析 EvalFile，并把 session/pending 中的同名项归一为同一个绝对路径，禁止相对默认名二次覆盖。
- **修复 NNUE 安装文件名**：Android 原生库安装目录使用规范名称 `libpikafish.nnue.so`。构建脚本仍接受源码目录中的 `pikafish.nnue.so`，打包时自动重命名为 `libpikafish.nnue.so`，提高覆盖安装后被提取到 `nativeLibraryDir` 的可靠性。
- **增强路径兼容与诊断**：自动查找同时兼容 `pikafish.nnue`、`pikafish.nnue.so`、`libpikafish.nnue.so`；每次真正发送 EvalFile 都记录完整命令，失败日志新增三个候选文件在安装目录中的存在状态和大小。
- **搜索前保护调整**：保留 `go infinite/go movetime` 前的最后一次 EvalFile 下发，但改为发送已验证的绝对路径，不再声称 `ucinewgame/isready` 会重置 UCI 选项。
- **版本同步**：应用版本号升级为 V15.0。

## V14.2 — 2026-07-18
- **开局库更新**：rp 开局库 `book.obk` 更新为 2026-07-18 最新版（4.5 MB）。


## V14.0 — 2026-07-17

- **编辑棋盘选中框彻底同步**：编辑模式下重复点击同一枚棋盘棋子会立即取消选择，绿色选中框同步消失；编辑模式不再绘制容易与选中框混淆的“上一手落点”绿色标记。
- **棋子按钮统一删除语义**：棋盘上已有棋子被选中时，点击 14 个棋子按钮中的任意一个，均删除当前选中棋子并归还数量池；即使目标按钮对应棋子数量已满、常规放置不可用，也仍可响应本次删除操作。
- **棋盘缩放留白配色统一**：棋盘缩小后左右两侧不再显示白色，改为与“黑方 · ……”信息栏、棋谱/引擎标签区域一致的浅绿色。
- **模式切换区配色统一**：包围“进入对弈模式 / 进入分析模式”按钮的区域改为相同浅绿色，棋盘页视觉衔接更一致。
- **版本同步**：应用版本号升级为 V14.0。

## V13.0 — 2026-07-17

- **已结束对局返回规则重构**：正式对弈结束后使用独立终局状态，不再因棋谱前后导航、查看其他局面或切换分析/对弈模式而丢失。返回时固定提示“确定返回初始界面吗？已下完的棋局不会保存！”，提供“取消 / 确认 / 退出但保留棋谱”三种操作；确认退出会清除棋谱，保留退出可在初始页重新查看。
- **终局操作锁定**：胜、和、负结算后，“提和”“认输”“悔棋”统一置灰并禁止点击；进入分析模式再切回对弈模式也不会重新启用或产生额外请求。
- **对弈旧线覆盖确认**：对弈进行中回看此前轮到玩家的局面并走出新招法时，先提示“新走法会覆盖旧走法，是否继续？”。确认后覆盖旧线及相关分支，取消则恢复原局面且不落子。
- **分析转对弈分支检查**：从自主分析模式进入对弈模式前扫描全部棋谱分支；存在分支时提示仅保留当前主线路，确认后清除其他分支再进入，取消则保持原状态。
- **分支列表重构**：取消“主线”标签，线路编号固定为 A–Z、AA、AB……；点击分支只切换当前高亮线路，编号不随点击变化。长按分支先显示“删除 / 取消”，确认删除前再次提示会连同衍生分支删除。
- **分支悔棋保护**：在后续存在不同分支的局面执行悔棋时，先提示“悔棋会删除之后所有衍生分支，是否悔棋？”，确认后才删除，取消不改变棋谱。
- **编辑棋盘交互修复**：重复点击已选中的棋子按钮会回到普通移动状态；棋盘上已有棋子被选中时，点击任意棋子按钮会先删除该棋子并归还数量池，不再错误切换画笔。
- **版本同步**：应用版本号升级为 V13.0，并新增终局留存、固定分支编号等会话字段的兼容读写。

## V12.0 — 2026-07-17

- **对弈提和限制与认输功能**：完整回合数 ≤5 时"提和"按钮置灰不可用；回合数 >5 恢复正常。"粘贴"与"提和"间新增"认输"按钮，全程可用，按下弹出确认弹窗，确认后直接判负并与历史战绩衔接。
- **终局返回不留存**：对弈分出胜负或提和成功后，返回初始页面时弹窗内不再调用 `persistCurrentSession`，改为 `clearSavedSession` 清除棋谱留存，避免下次"开始对弈"仍可进入已结束的棋局。
- **无进攻子力自动和棋**：对弈模式每步后检查棋盘，若红黑双方均无炮/马/车/兵（卒）任一攻击性子力，自动判定"无进攻子力，和棋！"并弹出和棋提示。
- **难度调整**："业余顶尖"搜索命令从 `go nodes 40000` 改为 `go nodes 25000`。
- 版本号升级为 V12.0。

## V11.0 — 2026-07-17

- **关于应用**：开源致谢文本更新为"感谢Pikafish、Duffish、PikafishHCE、Tchess这几个开源项目，以及Qsll为本项目提供了重要的参考和帮助。"
- **编辑模式完善**：修正棋子图标红黑对应（drawable-nodpi 中 piece_r*/piece_b* 命名与文件实际颜色相反，已交换映射）；第一行黑方、第二行红方；棋子按钮增宽、图标加大（30dp）、数字字号上调至 10sp；新增"翻转"按钮。
- **MultiPV 分析优化**：引擎页不再显示"正在等待同一深度的 X 条主要变例……"占位文字。
- **返回初始界面弹窗**：对弈模式已分出胜负或提和成功后，弹窗改为"确定退出吗？已下完的棋局不会保存！"。
- **当时的重新打分修复尝试（V15 已更正根因）**：曾按“`ucinewgame`/`isready` 后 EvalFile 被重置”处理，在搜索前补发 `setoption name EvalFile`；V15 日志确认真正问题是相对路径覆盖与 NNUE 未稳定提取。
- **查询：重新打分 UCI 配置**
  - 引擎实例：`rescoreEngine = new PikafishEngine(this, false)`（`loadPersistentOptions=false`）
  - 虚位置：`131`（`lib131.so`）
  - 构造函数默认值：`Threads=1, Hash=64, MultiPV=1, UCI_ShowWDL=true, Repetition Rule=SkyRule`
  - 手动选项：`applyStoredManualOptions(rescoreEngine)` 从 `human_vs_engine_v1` 读取 `manual_uci::` 前缀键，以 session 选项覆盖默认值
  - EvalFile：`configureEvalFileForEngine()` 自动解析 `nativeLibraryDir/pikafish.nnue.so`，V11 在搜索前补发 `setoption`
  - 搜索命令：`go movetime <用户输入毫秒数>`

## V10.0 — 2026-07-17

- **编辑模式重构**：棋子按钮从汉字改为象棋图标（`drawable-nodpi/piece_*.png`），红黑双行配剩余/上限计数。进入编辑时隐藏工具栏/标签/模式切换/Tab，棋盘全宽置顶，仅保留棋子选择+操作按钮；退出后全部恢复。
- **当时的覆盖安装修复尝试（V15 已更正根因）**：曾判断 EvalFile 被 `ucinewgame`/`isready` 重置，并在 `go infinite`/`go movetime` 前补发；V15 证明当时补发的仍可能是相对值 `pikafish.nnue`，且非 `lib` 前缀网络文件可能未进入安装目录。
- 重构棋盘页"新建"逻辑：无论棋谱是否为空均不再弹窗。对弈模式直接新建并保留当前难度、电脑执子与先后手设置；自主分析模式直接新建分析棋局并保持当前模式。
- 编辑模式下再次点击顶部"编辑"按钮直接视为完成，不再显示保存/放弃/继续编辑弹窗；只有局面校验不通过时才阻止退出并提示。

## V9.0 — 2026-07-17

- 修复粘贴 PGN 格式棋谱时 `PatternSyntaxException` 崩溃——`extractComments()` 和 `tokenizeMoves()` 中花括号正则末尾 `}` 在 Java regex 必须转义为 `\}`。同时增加全角空格和尾部非着法行过滤。
- 局势图终局补分：分析模式下导航到绝杀末局时局势图最后一点显示 0 分——引擎对无合法着法局面返回 `bestmove (none)` 无评分。修复：`navigateToPly()` 末局检测无合法着法时直接补 ±6000 分；`applySituationScoreResult()` 引擎未返回分数时同样从棋盘状态推导终局分数。
- 修复对弈界面开启"分析"后使用"立即出招"走到绝杀等终局时仍弹出胜负窗口的问题：分析终局仅更新棋谱结果和局势图，不弹窗、不清除留存，也不写入历史统计。
- 重构粘贴棋谱识别：中文着法改为枚举当前局面的全部合法着法，并与剪贴板记谱归一化后进行唯一匹配；任何错误导致整次粘贴直接失败，不再跳过错误后继续解析。
- 增强剪贴板兼容性，支持 PGN 标签大小写混用、繁简体棋子名、中文/半角/全角数字、无空格连续着法以及"初始FEN："文本格式。
- 重新打分改为纯分析流程：完成或中断后不再自动恢复电脑走棋；增加终局结算防护。
- 增加单局战绩幂等保护，同一盘棋只允许写入一次历史战绩。
- 棋谱滚动条布局完成后自动跟随当前局面。
- 分析模式和对弈模式下棋谱为空时，按系统返回键直接返回初始界面。
- 应用界面版本、Gradle 版本、引擎说明与源码标识同步更新为 V9.0。

## V8.0 — 2026-07-17

- **编译修复**：覆盖安装后分析引擎退出码 1——根因是 `applyStoredManualOptions()` 加载的旧版本 `manual_uci::` 前缀 session 选项污染了引擎参数。**解决方案**：`clearStaleEnginePrefsOnUpgrade()` 清理 `pikafish_engine_options` 和所有 `manual_uci::` 键。
- 最高难度"最新强软"更名为"青云"。
- 棋谱、引擎和局势图三个页面统一显示"首局、前、后、末局"导航按钮。
- 修复 MultiPV 分析只显示最新内容的问题：按深度倒序保留并展示所有 PV 的完整历史。
- 新增分析棋谱与未完成对局的独立自动留存：每次新走一步即保存，后台被杀、主动退出或返回初始页后均可恢复。
- 初始页进入分析模式时自动恢复上次分析棋谱；开始对弈时发现未完成记录会显示难度，并提供"新的对局 / 继续对局"。
- 对弈模式虚拟位 `131` 独立评分同时检查连续不吃子步数；评分达到 119 步后提示"60回合不吃子，和棋！"。
- 终局弹窗统一使用新 UI 及"确定 / 再来一局 / 新的一局"按钮；再来一局保持难度与引擎执子不变。
- 初始页新增按难度分别统计的"x胜x和x负"历史战绩框及重置确认功能。
- "关于应用"开源致谢名称规范为 Pikafish、Duffish、PikafishHCE；顶部"设置"按钮图标由扳手改为齿轮。
- 应用界面、Gradle 版本、源码说明与更新日志同步更新为 V8.0。

## V7.0 — 2026-07-16

- 对弈模式中，电脑执红完成第一步后允许悔棋回到初始局面。
- 对弈模式每完成一步，固定调用虚拟位 `131` 引擎、4 线程、`go movetime 100` 独立评分。
- 当前局面开启"分析"后，使用 MultiPV=1 主分支的实时分数更新局势图。
- 分析模式按返回键新增确认弹窗。
- 修复 UCI `MultiPV > 1` 时多个 PV 在相同深度互相覆盖的问题；引擎页按分支同时显示多条主要变例。
- "日志信息"窗口打开后默认滚动到最下方。
- "关于应用"信息改为单行显示版本号和作者。
- 应用版本号、Gradle 版本与源码说明同步更新为 V7.0。

## V6.0 — 2026-07-16

- 放大镜分析页采用 v68 显示格式：按深度倒序显示深度、分数、时间、NPS、节点、HF、WDL、不吃子步数和中文主要变例。
- 自主分析模式点击"新建"时直接在棋盘页创建新局，不再返回初始界面。
- 自主分析模式按系统返回键时直接退回初始界面。
- 从自主分析模式切换到对弈模式时，使用初始界面难度拖动条当前保存的难度。
- "关于应用"改为与应用主色一致的圆角卡片式界面。
- 界面版本、Gradle 版本和源码说明统一更新为 V6.0。

## V5.0 — 2026-07-16

- 修复 AMRv8 静态 ELF 引擎"直接 exec"被内核拒绝的问题：`EngineProbe` 对静态 EXEC 无条件走 APK 内置映射器。
- 修复棋盘顶部"分析"长期停留在"等待引擎输出"的问题：增加引擎提前退出/超时错误处理；`go infinite` 无输出时自动切换循环有限时搜索。
- lib131.so 不存在时，分析、重新打分和提和自动回退到"引擎设置"中当前选择的可用引擎。
- 重新打分调用前增加引擎可用性检查与 `isready/readyok` 同步。
- 棋谱页左侧每步格子改为按实际文字宽度计算。
- 棋盘界面的"设置"新增"调整棋盘大小"。
- "版本信息"改为"关于应用"，加入开源项目致谢。

## V4.0 — 2026-07-16

- 分析引擎增加多级重试机制：首次 3.5 秒无 info → 重来；二次 5 秒仍无 → 全量重启进程；之后每 15 秒检查存活性。
- 棋谱翻页时滚动条修复：`post()` 改为 `addOnLayoutChangeListener`，消除先闪到顶部的回弹 bug。
- 重新打分前增加 `ensureRescoreEngineAlive()`，检查引擎 executor 可用性。
- 版本信息对话框增加"特别鸣谢：ChatGPT5.6，DeepSeekV4pro"。

## V3.0 — 2026-07-16

- 进入局面编辑时临时缩小棋盘，确保底部控制栏始终有显示空间。
- 顶部工具栏改为紧凑的自绘矢量图标，不依赖 Emoji 字体。
- "复制局面/复制棋谱"合并为"复制"图标。"粘贴"自动识别 FEN、moves、中文棋谱或 PGN。
- 棋谱右侧去掉独立"分支"标题，增加首局、前、后、末局四个小导航按钮。
- 棋谱列表改为每行显示一个完整回合。
- 开局库改为在当前局面全部合法候选中随机选择。
- 版本信息增加"作者：赵芮"。
- 初始页增加"分析模式"：双方均由玩家走棋，不自动调用对弈引擎。
- 点击"提和"后立即显示不可取消的"分析中"窗口。
- 棋盘上方增加固定的"进入分析模式/进入对弈模式"切换按钮。
- "重新打分"的每步用时输入框默认为空。
- 棋谱/引擎/局势图标签以及重新打分/停止按钮整体缩小。
- 引擎信息简化为 depth、红黑分、WDL、Nodes、K NPS、Hash% 和自适应时间单位。
- 进入棋盘页面后保持亮屏，返回初始页恢复系统正常亮屏策略。
- 初始页增加"调整棋盘大小"：范围 50%–100%，拖动实时预览。

## V2.0 — 2026-07-16

- "引擎设置"直接读取并展示引擎返回的全部 UCI 选项。
- "分析"使用 `go infinite`，由用户再次点击"分析"停止；"立即出招"和"变招"复用当前无限分析结果。
- 对弈页增加系统状态栏安全边距，默认始终把电脑放在棋盘上方。
- 编辑面板改为紧凑布局，移动/删除/棋子、红先/黑先均以绿色标识当前状态。
- 对弈页保留"棋谱/引擎/局势图"三个标签。
- 局势图恢复 v68 的分段分值坐标、阶段线、拖动定位与独立重新打分流程。
- 棋谱导航保持原滚动位置；从历史局面走出不同着法时保留分支。
- 增加终局胜负提示、复制局面、复制棋谱、粘贴、提和和悔棋。
- 提和固定使用独立 131 引擎、8 线程、2 秒；电脑视角优势不超过 200 分时同意和棋。

## V1.0 — 2026-07-16

- 首个版本发布，包名 `com.tyl.xiangqi.nodechess`。
- 启动页显示 V1.0、引擎执红/黑选择、分段式难度滑条、开始对弈与设置。
- 三种内置引擎虚位置：HCE、duf、131。
| 难度 | 虚位置 | 脱库回合 | 线程 | 搜索命令 |
|---|---:|---:|---:|---|
| 略知一二 | HCE | 3 | 2 | `go depth 2` |
| 棋摊中坚 | duf | 5 | 2 | `go nodes 800` |
| 街头霸王 | duf | 5 | 2 | `go nodes 1000` |
| 县镇好手 | duf | 8 | 2 | `go nodes 4600` |
| 业余顶尖 | duf | 10 | 2 | `go nodes 40000` |
| 特级大师 | duf | 10 | 2 | `go nodes 70000` |
| 人类+ | duf | 5 | 2 | `go nodes 900000` |
| 最新强软 | 131 | 1 | 4 | `go nodes 4500000` |

## 下次编译时放入引擎与开局库

```text
app/src/main/pikafish/arm64-v8a/libHCE.so
app/src/main/pikafish/arm64-v8a/libduf.so
app/src/main/pikafish/arm64-v8a/lib131.so
app/src/main/assets/books/rp/book.obk
app/src/main/pikafish/arm64-v8a/pikafish.nnue.so
app/src/main/pikafish/arm64-v8a/xiangqi-agg20260512.nnue.so
```

- 构建时会将 `pikafish.nnue.so` 自动重命名为 APK 内的 `libpikafish.nnue.so`，并将 `xiangqi-agg20260512.nnue.so` 自动重命名为 `libxiangqi-agg20260512.nnue.so`。
- 统一 rp 开局库读取，仅由固定难度自动对弈流程使用。
- 分析、立即出招、变招始终调用独立的 131 引擎实例。
- NNUE 网络文件适配 Android 构建链：命名为 `.nnue.so` 后缀以参与 nativeLibsToJar 打包。

---

> 应用包名：`com.tyl.xiangqi.nodechess`
> 最低 Android：6.0 (API 23) · JDK 17 · compileSdk 35
> 三个引擎槽位：HCE / duf / 131
